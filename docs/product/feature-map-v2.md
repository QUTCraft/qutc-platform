# QUTCraft Commons 功能地图 v2

> 状态基线：2026 年 7 月 25 日
>
> 目标版本：`v0.1.0-mvp`（2026 年 8 月 31 日）
>
> 关联文档：[项目排期](../../schedule.md)、[需求范围 v1](requirements-v1.md)、[MVP 验收清单](mvp-acceptance.md)、[OpenAPI 契约](../api/openapi.yaml)

## 1. 这份功能表解决什么问题

QUTCraft Commons 已从“门户原型”进入多模块并行开发阶段。单纯按页面名称记录进度，容易把“界面存在”“接口存在”“完整闭环可用”混为一谈。本表将每项能力拆成三个层次：

1. **当前实现**：以仓库中的前端、Go 路由、OpenAPI 和测试为准。
2. **MVP 收口**：8 月 31 日前必须达到的可演示、可验收状态。
3. **扩展方向**：保留接口与数据模型空间，但不默认进入当前迭代。

公开门户仍是独立宣传官网，不是后台的只读镜像。管理端负责内容生产和组织协作；自定义门户只能使用公开 Portal API。

## 2. 标记与优先级

### 2.1 实现状态

| 标记 | 含义 | 判定标准 |
| --- | --- | --- |
| ✅ | 已形成基础闭环 | 页面与真实 API 已连接，主要成功路径可运行。 |
| 🟡 | 部分完成 | 页面、接口、契约或持久化中至少一层缺失。 |
| ⬜ | 未实现 | 只有需求描述，尚无可用入口。 |
| 📐 | 仅有契约/设计 | 已有规范、类型或原型，但运行时尚未实现。 |
| ➕ | 扩展项 | 不属于 `v0.1.0-mvp` 的发布阻断项。 |

状态表示“仓库当前事实”，不是对质量的永久承诺。功能只有符合本文第 9 节的完成定义，才能在发布检查中标为完成。

### 2.2 优先级

| 优先级 | 定义 | 调度规则 |
| --- | --- | --- |
| P0 | 发布阻断 | 三条核心闭环或安全边界所必需；优先修复。 |
| P1 | MVP 增强 | 提升完整度与比赛表达；不能挤占未完成 P0。 |
| P2 | 版本扩展 | 进入 `v0.2+`，先保留 API/模型扩展点。 |

## 3. MVP 的三条核心闭环

```text
内容闭环：后台创建/编辑 → 发布 → Portal API → 门户展示 → 下线后消失

申请闭环：门户提交申请 → 后台审批 → 审计记录 → Mock/RCON Adapter → 明确结果

门户闭环：选择 Portal Manifest → 加载公开能力 → 主题呈现 → 失败时回退 MD3
```

8 月 31 日的 MVP 必须优先保证这三条链路连续三次成功。成员、项目、知识库和资源能力用于支撑这三条主线，而不是把后台扩展成通用 OA。

## 4. 功能总览

| 领域 | 当前状态 | MVP 目标 | 优先级 | 主要依赖 |
| --- | --- | --- | --- | --- |
| 公开宣传门户 | ✅ 基础页面与公开数据已接入 | 完成内容详情、真实申请与一致的空错状态 | P0 | Portal API、内容发布 |
| 身份与会话 | ✅ JWT/RBAC 基础可用 | 补齐权限回归、会话安全和异常路径 | P0 | 用户、成员关系、审计 |
| 内容 CMS | ✅ 基础发布闭环可用 | 固化状态机、资源关联、缓存失效与测试 | P0 | 内容、资产、Redis |
| 资源与文件 | 🟡 上传/下载基础接口可用 | 元数据、鉴权、引用关系和可替换存储 | P0/P1 | 资产服务、内容 CMS |
| 成员管理 | 🟡 可查看和修改，邀请缺失 | 邀请、角色变更、停用与审计闭环 | P0 | 身份、邮件可选适配器 |
| 项目管理 | 🟡 项目 UI 可用，成员/里程碑仅后端 | 管理界面接入成员和里程碑接口 | P1 | 成员、RBAC |
| 知识库 | 🟡 公开目录和内容复用可用 | 独立目录维护、文章编辑和发布体验 | P1 | 内容 CMS、搜索后置 |
| 申请与审批 | 🟡 公开提交、数据库列表和事务化审批已完成；适配器同步仍未接入 | 完成申请闭环与 Mock/真实适配器结果分离 | P0 | 身份可选、ServerAdapter |
| 服务器适配 | 🟡 Mock 命令白名单与审计存在 | 抽象适配器、可靠状态与真实 RCON 可选接入 | P0/P1 | 申请审批、服务端配置 |
| 自定义门户 | 📐 Manifest/Token 规范已完成 | 加载、预览、启用、兼容检查和回退 | P0 | Portal API 稳定版 |
| 系统设置 | 📐 当前为演示 UI | 持久化组织/门户/适配器配置，密钥仅后端保存 | P1 | 配置模型、审计 |
| 工程质量 | 🟡 Compose/OpenAPI 已有，测试稀少 | 契约校验、关键集成测试、安全与可观测性 | P0 | 所有核心模块 |

## 5. 分领域功能表

### 5.1 公开宣传门户

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 组织首页 | 已接组织信息、动态、项目、知识、资源和服务器公开状态 | 远程 API 模式下加载/空/错状态完整；不出现后台工作流 | 模块化首页区块、组织自选排序 | P0 |
| 动态列表 | 已有 `/posts` 与公开列表 | 已发布内容可见，下线内容不可见；增加详情入口或明确列表即终点 | 标签、置顶、定时发布、SEO | P0 |
| 项目目录 | 已有 `/projects` 与公开项目 | 只返回公开字段；与后台公开开关一致 | 项目详情、成果时间线、贡献者公开授权 | P1 |
| 资源中心 | 已有 `/resources` 与下载入口 | 下载来自服务端授权地址；失败提示和移动端布局可用 | 版本、镜像、下载统计、许可证 | P0 |
| 知识库 | 已有 `/knowledge` 与目录数据 | 公开文章可浏览，草稿不可见；目录为空时有明确状态 | 全文检索、历史版本、关联文章 | P1 |
| 加入申请 | Portal POST、OpenAPI、数据库落库、重复待审冲突和后台列表已接入 | 提交真实持久化申请并在后台出现；重复提交有明确信息 | 补充材料、进度查询、撤回、反垃圾 | P0 |
| 服务器公开卡片 | 基础脱敏状态可读 | 仅返回在线、人数、版本、同步时间；适配器不可用时不伪报在线 | 多服务器、历史可用率、活动日历 | P1 |
| 详情与发现 | 目前以列表/卡片为主 | 至少保证内容可完整阅读；统一 404 | 搜索、RSS、站点地图、分享卡片 | P1/P2 |

### 5.2 身份、账户与权限

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 注册、登录、刷新、退出 | Go API 与前端客户端已存在 | 正常、过期、错误密码、Refresh 失效均有测试和统一错误 | OAuth/校园统一认证 | P0 |
| 当前用户与资料 | 查询/修改接口已存在 | 字段校验、邮箱隐私和修改审计明确 | 头像、偏好、公开资料页 | P1 |
| JWT 会话 | Access/Refresh 基础实现已存在 | 密钥禁止默认值进入生产；退出后 Refresh 不可复用 | 多设备会话、主动撤销、设备列表 | P0 |
| RBAC | 中间件与权限矩阵已存在 | 对内容、成员、审批、服务器命令做服务端越权测试 | 组织自定义角色、细粒度资源策略 | P0 |
| 账号恢复 | 尚未实现 | 不阻断封闭演示；交付文档说明管理员恢复流程 | 邮箱验证、忘记密码、2FA | P2 |

### 5.3 成员与组织关系

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 成员列表 | 管理端和 API 可用 | 分页、空态、权限拒绝和隐私字段边界明确 | 筛选、批量操作、导出 | P0 |
| 角色/状态修改 | 页面与 API 已接入 | 禁止越权修改 Owner；变更写审计；停用后权限立即失效 | 临时角色、角色到期 | P0 |
| 邀请成员 | 页面按钮为占位，无接口 | 新建邀请、唯一/过期 Token、接受邀请、重复邀请冲突 | 邮件投递、批量邀请、邀请模板 | P0 |
| 成员历程/退出 | 当前用户历史与退出接口已存在 | 退出后的访问权限和历史保留可验证 | 重新加入、组织间迁移 | P1 |
| 成员档案 | 仅基础账户字段 | MVP 保持内部最小字段，不强制公开成员墙 | 技能、部门、贡献、隐私授权 | P2 |

### 5.4 内容 CMS、知识库与资源

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 草稿创建/编辑 | 管理端与 API 已接入 | 校验、保存失败提示、刷新后数据不丢失 | 自动保存、协同编辑 | P0 |
| 发布/下线 | 基础接口已接入，并触发公开缓存失效 | 状态机固定；发布后可见、下线后不可见；写审计 | 定时发布、审核流、回滚 | P0 |
| 内容审核 | `review` 状态有模型语义，但无完整操作流 | MVP 可采用“具备发布权限者直接发布”，文档明确 | 多级审核、修改意见、审批 SLA | P2 |
| 文章正文 | 当前支持纯文本 | 保证安全转义和完整阅读 | Markdown/富文本、嵌入组件 | P1/P2 |
| 资产上传/下载 | 基础上传、下载接口和本地卷存在 | 类型/大小校验、权限、元数据、内容引用可验证 | MinIO/S3、签名 URL、分片上传 | P0/P1 |
| 知识目录 | 目录接口已存在 | 管理端可创建/编辑目录，文章能关联目录 | 树形目录、移动、别名 | P1 |
| 搜索与版本 | 未实现 | 不阻断 MVP | 全文索引、修订历史、差异与恢复 | P2 |

### 5.5 项目协作

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 项目创建/编辑/公开 | 管理界面和 Go API 已接入 | 负责人、状态、标签和公开范围保存一致 | 项目模板、自定义字段 | P1 |
| 项目成员 | Go CRUD 接口已有，管理界面未接入 | 在项目详情/抽屉中添加、改角色、移除；Owner 不可误删 | 邀请外部协作者、成员工时 | P1 |
| 项目里程碑 | Go CRUD 接口已有，管理界面未接入 | 创建、更新、完成、删除和日期校验可操作 | 依赖、提醒、进度计算 | P1 |
| 项目任务 | 未实现 | 不进入 MVP | 看板、Issue、指派、评论、附件 | P2 |
| 组合视图 | 未实现 | 门户只展示公开项目摘要 | 路线图、甘特图、跨项目统计 | P2 |

### 5.6 申请、审批与服务器适配

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 申请提交 | Portal API、OpenAPI、持久化模型、字段校验和重复待审冲突已完成 | 继续补充端到端测试和适配器结果 | 附件、问卷模板、验证码 | P0 |
| 申请列表 | 已改为数据库分页与组织隔离 | 继续补齐详情展示与筛选 | 条件筛选、批量审批 | P0 |
| 批准/拒绝 | 已使用数据库事务、并发 `409` 和审计记录 | 审批人与时间持久化；服务器同步结果仍需独立记录 | 转交、补充材料、撤销决定 | P0 |
| ServerAdapter | 尚未形成明确接口层 | 定义状态、命令、白名单变更的统一接口 | 多游戏/多服务适配器 | P0 |
| Mock Adapter | 当前服务器命令返回明确“未连接真实 RCON”，带命令白名单 | 申请审批闭环可重复运行且不伪报真实执行 | 故障注入、延迟模拟 | P0 |
| Minecraft RCON | 尚未接入真实连接 | 测试服可用时接入；不可用不阻断 MVP，但必须明确 Mock 标识 | 多实例、连接池、自动恢复 | P1 |
| 命令安全 | 已有少量白名单、换行/长度限制和审计 | 参数规则、超时、错误脱敏、权限测试 | 审批制高危命令、策略配置 | P0 |
| 通知 | 设置页包含 SMTP 演示表单，无后端持久化/发送 | MVP 可先提供站内状态；若接 SMTP，凭据只存后端 | 邮件、Webhook、企业微信等渠道 | P1/P2 |

### 5.7 自定义门户与组织配置

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| Manifest v1 | Schema、能力边界和示例文档已完成 | 校验器能拒绝不兼容字段/能力 | 签名、市场、依赖声明 | P0 |
| 默认 MD3 门户 | 当前 Vue 门户可用 | 作为永远可用的安全回退 | 组织区块编排 | P0 |
| Minecraft 门户 | 有 HTML 风格稿与现有 QUTCraft 视觉 | 作为第一方自定义门户样例接同一公开 API | 社团素材包、活动皮肤 | P1 |
| 加载与预览 | 尚无运行时实现 | 管理端预览、启用、超时/错误回退，不改管理端路由 | 灰度、版本回滚、A/B 测试 | P0 |
| 组织基础设置 | 设置页当前仅本地演示 | 名称、公开状态、门户选择持久化并审计 | 自定义域名、品牌资源 | P1 |
| 扩展安全 | 文档已规定公开能力白名单 | 自定义门户不能访问 Admin API、内部凭据和未发布数据 | CSP 沙箱、资源完整性校验 | P0 |

### 5.8 工程、部署与质量

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| Docker Compose | Web/API/MySQL/Redis 可启动，MinIO/Swagger 使用 profile | 新机器按 README 一次启动，健康检查与端口覆盖可用 | 分环境 Compose、Kubernetes | P0 |
| OpenAPI / Apifox / Swagger | 契约较完整，但存在申请接口错位 | OpenAPI 是事实来源；CI 校验；Apifox 可导入并跑核心用例 | 自动生成 SDK、契约差异门禁 | P0 |
| 数据迁移 | MySQL 迁移已有基础 | 新功能只通过迁移改表；升级和回滚说明明确 | 在线迁移、迁移审计 | P0 |
| Redis 缓存 | 公开数据缓存与部分失效已存在 | 发布、下线、项目公开变更后不会读到陈旧数据 | 分层缓存、热点保护 | P1 |
| 自动化测试 | 当前仅有少量认证单测 | 增加三条闭环的服务测试和 Compose 冒烟测试 | 前端 E2E、性能、故障演练 | P0 |
| 可观测性 | 有 `request_id` 与部分审计 | 统一结构化日志、关键写操作审计、健康检查 | 指标、追踪、告警 | P0/P1 |
| 安全基线 | 已有 RBAC、CORS、部分输入/命令校验 | 密钥检查、上传校验、限流、越权测试、日志脱敏 | SAST/DAST、依赖签名 | P0 |
| 备份与恢复 | 尚未形成流程 | 至少提供 MySQL/媒体卷备份恢复脚本与演练记录 | 增量备份、异地恢复 | P1 |
| CI/CD | 尚未形成完整门禁 | 最少执行 Go test、前端构建和 OpenAPI 校验 | 自动部署、预览环境 | P1 |

## 6. 扩展路线

扩展项按价值和依赖分层，不按“看起来酷”决定先后。

### v0.2：运营与协作增强

- 内容详情、Markdown/基础富文本、修订历史和定时发布。
- 成员档案、批量邀请、邀请邮件和成员筛选。
- 项目任务看板、评论、附件和里程碑提醒。
- 知识库全文搜索、文章关联和导入/导出。
- 资源版本、下载统计、MinIO/S3 签名 URL。
- SMTP/Webhook 通知适配器及失败重试。

进入条件：`v0.1.0-mvp` 三条闭环稳定，权限与数据隔离测试通过，OpenAPI 不再频繁破坏性变更。

### v0.3：多组织与生态适配

- 真正的多组织切换、租户级配置、配额与数据隔离回归。
- 多服务器、多个 `ServerAdapter` 实例和统一状态聚合。
- 门户版本管理、灰度、回滚、自定义域名和可安装主题包。
- 组织级 Webhook、外部身份源和标准化数据导入。

进入条件：单组织部署完成真实使用验证；组织隔离、审计和配置模型经安全评审。

### v1.0：平台化能力

- 插件/适配器市场，但只接受声明式或受沙箱限制的扩展。
- 工作流编排、自定义表单、自定义角色和策略授权。
- 高可用部署、对象存储集群、异地备份和完整监控体系。
- 面向比赛与真实社团部署的版本治理、升级兼容和长期支持策略。

进入条件：扩展安全模型、版本兼容策略、运维责任和插件审核机制全部明确。不得以“执行任意第三方后端代码”作为捷径。

## 7. 依赖与开发顺序

| 能力 | 必须先稳定的依赖 | 原因 |
| --- | --- | --- |
| 内容/知识/资源公开 | 内容状态机、公开 DTO、缓存失效 | 防止草稿或内部字段泄露。 |
| 成员邀请 | 用户唯一性、成员关系、Token 生命周期、RBAC | 避免重复账户和越权加入。 |
| 项目成员/里程碑 UI | 项目权限、活跃成员列表、现有 CRUD API | 页面不能绕过服务端项目范围。 |
| 审批到服务器 | 申请持久化、审批事务、审计、ServerAdapter | 防止“批准成功但服务器未执行”的假成功。 |
| 自定义门户运行时 | Portal API 冻结、Manifest 校验、默认 MD3 回退 | 避免主题绑死内部接口或拖动核心契约。 |
| 通知 | 业务事件可靠持久化、密钥配置 | 通知失败不能导致业务状态回滚或密钥泄露。 |

## 8. 取舍规则

1. P0 未通过阶段门时，不开始新的 P2 页面。
2. “接口已写”不等于完成；前端、服务端、契约、持久化和测试必须分别标记。
3. 外部依赖不可用时使用明确标识的适配器或降级状态，不伪造真实成功。
4. MVP 可减少编辑器、搜索、通知等体验增强，但不能削弱权限、审计、数据隔离和回退。
5. 新需求先归入本表的领域与版本；若会改变 P0 路径，必须同步调整 [schedule.md](../../schedule.md)。

## 9. 功能完成定义

一项功能只有同时满足以下条件，才可从 🟡/📐 改为 ✅：

- 用户入口和成功、空、失败、冲突/权限路径均有明确表现。
- OpenAPI、Go 实现和 TypeScript client 字段一致。
- 数据需要持久化时不依赖进程内临时状态或前端常量。
- 服务端执行真实授权，敏感字段不进入 Portal API。
- 至少有一条自动化测试，核心闭环另有 Compose 环境冒烟记录。
- README、功能表和相关规范已同步，不以 Mock 冒充生产能力。

## 10. 当前最先处理的错位

1. 为已完成的门户申请和审批闭环补齐 Compose 集成测试与 Apifox 用例。
2. 实现成员邀请，替换管理端没有事件处理的占位按钮。
3. 把已有的项目成员、项目里程碑 API 接入项目管理界面。
4. 抽象 `ServerAdapter`，把 Mock/RCON 执行结果与审批状态分开记录。
5. 为系统设置增加真实 API；移除前端示例 SMTP 密钥，所有密钥只由服务端环境或受控密钥存储读取。
6. 建立三条核心闭环的测试，作为后续扩展进入条件。
