# QUTCraft Commons 功能地图 v2

> 状态基线：2026 年 7 月 28 日
>
> 目标版本：核心基线 `v0.1.0-mvp`；最终比赛版 `v0.1.0-competition`（2026 年 8 月 31 日）
>
> 关联文档：[项目排期](../../schedule.md)、[需求范围 v1](requirements-v1.md)、[MVP 验收清单](mvp-acceptance.md)、[OpenAPI 契约](../api/openapi.yaml)

## 1. 这份功能表解决什么问题

QUTCraft Commons 已从“门户原型”进入多模块并行开发阶段。单纯按页面名称记录进度，容易把“界面存在”“接口存在”“完整闭环可用”混为一谈。本表将每项能力拆成三个层次：

1. **当前实现**：以仓库中的前端、Go 路由、OpenAPI 和测试为准。
2. **MVP 收口**：8 月 31 日前必须达到的可演示、可验收状态。
3. **扩展方向**：保留接口与数据模型空间，但不默认进入当前迭代。

公开门户仍是独立宣传官网，不是后台的只读镜像。管理端负责内容生产和组织协作；自定义门户只能使用公开 Portal API。

## 2. 标记与优先级

### 2.1 实现状态

| 标记 | 含义 | 判定标准 |
| --- | --- | --- |
| ✅ | 已形成基础闭环 | 页面与真实 API 已连接，主要成功路径可运行。 |
| 🟡 | 部分完成 | 页面、接口、契约或持久化中至少一层缺失。 |
| ⬜ | 未实现 | 只有需求描述，尚无可用入口。 |
| 📐 | 仅有契约/设计 | 已有规范、类型或原型，但运行时尚未实现。 |
| ➕ | 扩展项 | 不属于 `v0.1.0-mvp` 的发布阻断项。 |

状态表示“仓库当前事实”，不是对质量的永久承诺。功能只有符合本文第 9 节的完成定义，才能在发布检查中标为完成。

### 2.2 优先级

| 优先级 | 定义 | 调度规则 |
| --- | --- | --- |
| P0 | 发布阻断 | 三条核心闭环或安全边界所必需；优先修复。 |
| P1 | MVP 增强 | 提升完整度与比赛表达；不能挤占未完成 P0。 |
| P2 | 版本扩展 | 进入 `v0.2+`，先保留 API/模型扩展点。 |

## 3. MVP 的三条核心闭环

```text
内容闭环：后台创建/编辑 → 发布 → Portal API → 门户展示 → 下线后消失

申请闭环：门户提交申请 → 后台审批 → 审计记录 → Mock/RCON Adapter → 明确结果

门户闭环：选择 Portal Manifest → 加载公开能力 → 主题呈现 → 失败时回退 MD3
```

8 月 31 日的 MVP 必须优先保证这三条链路连续三次成功。成员、项目、知识库和资源能力用于支撑这三条主线，而不是把后台扩展成通用 OA。

## 4. 功能总览

| 领域 | 当前状态 | MVP 目标 | 优先级 | 主要依赖 |
| --- | --- | --- | --- | --- |
| 公开宣传门户 | ✅ 基础页面与公开数据已接入 | 完成内容详情、真实申请与一致的空错状态 | P0 | Portal API、内容发布 |
| 身份与会话 | ✅ JWT/RBAC 基础可用 | 补齐权限回归、会话安全和异常路径 | P0 | 用户、成员关系、审计 |
| 内容 CMS | ✅ 基础发布闭环可用 | 固化状态机、资源关联、缓存失效与测试 | P0 | 内容、资产、Redis |
| 资源与文件 | 🟢 上传/下载、元数据、引用和公开受控下载已可用 | MinIO/S3 可替换存储、版本和统计 | P0/P1 | 资产服务、内容 CMS |
| 成员管理 | 🟢 成员查看/修改、邀请注册与接受已可用 | 批量邀请、成员资料、通知和更细粒度审计 | P0 | 身份、邮件可选适配器 |
| 项目管理 | 🟢 项目 CRUD、成员/里程碑 API、管理协作抽屉与 S2 集成测试均已可用 | 批量操作与门户项目详情进入后续增强 | P1 | 成员、RBAC |
| 知识库 | 🟢 公开文章/目录、详情和 Admin 目录维护已可用 | 文章编辑体验、树形目录和搜索 | P1 | 内容 CMS、搜索后置 |
| 申请与审批 | ✅ 公开提交、数据库分页筛选、审核原因、事务化审批、独立同步结果、失败重试、显式演示 seed 和 S3 集成测试已完成 | MVP 主路径已闭环 | P0 | 身份可选、ServerAdapter |
| 服务器适配 | 🟡 ServerAdapter、明确 Mock 模式、命令白名单、统一超时、同步筛选、独立记录和故障恢复演练已存在 | 补运维告警；真实 RCON 暂缓 | P0/P1 | 申请审批、服务端配置 |
| 自定义门户 | ✅ Manifest/Token 校验、配置持久化、Admin 流程、公开运行时加载、入口探测、MD3 自动/手动恢复和 CSP 已完成 | 第一方主题已取消；第三方门户按通用包规范接入 | P0 | Portal API 稳定版 |
| 系统设置 | 🟡 门户设置已接真实 API；SMTP 明确为未持久化 | 后续持久化组织基础资料；密钥仅后端保存 | P1 | 配置模型、审计 |
| 工程质量 | 🟡 Compose/OpenAPI、路由契约校验、S1/S2 集成套件已存在 | 继续补申请适配闭环、安全与可观测性 | P0 | 所有核心模块 |

## 5. 分领域功能表

### 5.1 公开宣传门户

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 组织首页 | 已接组织信息、动态、项目、知识、资源和服务器公开状态 | 远程 API 模式下加载/空/错状态完整；不出现后台工作流 | 模块化首页区块、组织自选排序 | P0 |
| 动态列表 | 已有 `/posts`、`/content/{id}` 与公开详情 | 已发布内容可见，下线内容不可见；详情正文只返回公开字段 | 标签、置顶、定时发布、SEO | P0 |
| 项目目录 | 已有 `/projects` 与公开项目 | 只返回公开字段；与后台公开开关一致 | 项目详情、成果时间线、贡献者公开授权 | P1 |
| 资源中心 | 已有 `/resources`、资源详情、上传和受控下载 | 文件类型/大小、内容引用、无文件 `null`、公开下载和缓存失效均已验证 | 版本、镜像、下载统计、许可证 | P0 |
| 知识库 | 已有 `/knowledge/articles`、目录数据、文章详情和 Admin 目录编辑 | 公开文章可浏览，草稿不可见；目录为空时有明确状态 | 全文检索、历史版本、关联文章 | P1 |
| 加入申请 | Portal POST、OpenAPI、数据库落库、重复待审冲突和后台列表已接入 | 提交真实持久化申请并在后台出现；重复提交有明确信息 | 补充材料、进度查询、撤回、反垃圾 | P0 |
| 服务器公开卡片 | 基础脱敏状态可读 | 仅返回在线、人数、版本、同步时间；适配器不可用时不伪报在线 | 多服务器、历史可用率、活动日历 | P1 |
| 详情与发现 | 动态、资源、知识文章已有统一详情路由和 404 | 已发布内容可完整阅读；草稿/下线详情统一 404 | 搜索、RSS、站点地图、分享卡片 | P0/P1 |

### 5.2 身份、账户与权限

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 注册、登录、刷新、退出 | Go API 与前端客户端已存在 | 正常、过期、错误密码、Refresh 失效均有测试和统一错误 | OAuth/校园统一认证 | P0 |
| 当前用户与资料 | 查询/修改接口已存在 | 字段校验、邮箱隐私和修改审计明确 | 头像、偏好、公开资料页 | P1 |
| JWT 会话 | Access/Refresh 基础实现已存在 | 密钥禁止默认值进入生产；退出后 Refresh 不可复用 | 多设备会话、主动撤销、设备列表 | P0 |
| RBAC | 中间件与权限矩阵已存在 | 对内容、成员、审批、服务器命令做服务端越权测试 | 组织自定义角色、细粒度资源策略 | P0 |
| 账号恢复 | 尚未实现 | 不阻断封闭演示；交付文档说明管理员恢复流程 | 邮箱验证、忘记密码、2FA | P2 |

### 5.3 成员与组织关系

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 成员列表 | 管理端和 API 可用 | 分页、空态、权限拒绝和隐私字段边界明确 | 筛选、批量操作、导出 | P0 |
| 角色/状态修改 | 页面与 API 已接入 | 禁止越权修改 Owner；变更写审计；停用后权限立即失效 | 临时角色、角色到期 | P0 |
| 邀请成员 | Admin 按钮、邀请表、Go API 和注册/接受页面已接入 | token 哈希存储、默认 7 天过期、重复邀请冲突、邮箱匹配、Owner 不可授予 | 邮件投递、批量邀请、邀请模板、撤销/重发 | P0 |
| 成员历程/退出 | 当前用户历史与退出接口已存在 | 退出后的访问权限和历史保留可验证 | 重新加入、组织间迁移 | P1 |
| 成员档案 | 仅基础账户字段 | MVP 保持内部最小字段，不强制公开成员墙 | 技能、部门、贡献、隐私授权 | P2 |

### 5.4 内容 CMS、知识库与资源

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 草稿创建/编辑 | 管理端与 API 已接入 | 校验、保存失败提示、刷新后数据不丢失 | 自动保存、协同编辑 | P0 |
| 发布/下线 | 基础接口已接入，并触发公开缓存失效 | 状态机固定；发布后可见、下线后不可见；写审计 | 定时发布、审核流、回滚 | P0 |
| 内容审核 | `review` 状态有模型语义，但无完整操作流 | MVP 可采用“具备发布权限者直接发布”，文档明确 | 多级审核、修改意见、审批 SLA | P2 |
| 文章正文 | 已支持标准 Markdown、实时预览、文件导入、图片/附件插入和 Portal 安全渲染 | 保证媒体公开地址、HTML 清洗和完整阅读路径稳定 | 复杂富文本、嵌入组件、修订历史 | P1/P2 |
| 资产上传/下载 | 基础上传、下载接口、本地卷和 nonroot 权限初始化已接入 | 类型/大小校验、权限、元数据、内容引用和公开下载可验证 | MinIO/S3、签名 URL、分片上传 | P0/P1 |
| 知识目录 | 目录接口与管理端页面已接入 | 管理端可创建/编辑目录，文章可通过 `category` 关联目录 | 树形目录、移动、别名 | P1 |
| 搜索与版本 | 未实现 | 不阻断 MVP | 全文索引、修订历史、差异与恢复 | P2 |

### 5.5 项目协作

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 项目创建/编辑/公开 | 管理界面和 Go API 已接入 | 负责人、状态、标签和公开范围保存一致 | 项目模板、自定义字段 | P1 |
| 项目成员 | Go CRUD 接口与 Admin 项目协作抽屉已接入 | 添加、改角色、移除均调用真实 API；只能加入活跃组织成员，Owner 不可误删 | 邀请外部协作者、成员工时、批量分配 | P1 |
| 项目里程碑 | Go CRUD 接口与 Admin 项目协作抽屉已接入 | 创建、更新、完成、删除和 RFC3339 日期校验可操作；计数随变更刷新 | 依赖、提醒、进度计算 | P1 |
| 项目任务 | 未实现 | 不进入 MVP | 看板、Issue、指派、评论、附件 | P2 |
| 组合视图 | 未实现 | 门户只展示公开项目摘要 | 路线图、甘特图、跨项目统计 | P2 |

### 5.6 申请、审批与服务器适配

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 申请提交 | Portal API、OpenAPI、持久化模型、字段校验和重复待审冲突已完成 | 继续补充端到端测试和适配器结果 | 附件、问卷模板、验证码 | P0 |
| 申请列表 | 已完成数据库分页、组织隔离、状态/类型/同步状态筛选、关键字段搜索和审核原因展示 | 页面展示同步时间、尝试次数和脱敏错误 | 批量审批、保存筛选视图 | P0 |
| 批准/拒绝 | 已使用数据库事务、并发 `409` 和审计记录 | 审批人与时间持久化；服务器同步结果仍需独立记录 | 转交、补充材料、撤销决定 | P0 |
| ServerAdapter | 已定义状态、受限命令和白名单写入接口，具备统一超时、失败重试与独立持久化 | 收口 Mock 健康恢复、同步筛选和运维告警；真实 RCON 不进入当前 MVP | 多游戏/多服务适配器 | P0 |
| Mock Adapter | 可重复模拟状态、命令和白名单；明确 `mode=mock`、`executed=false` | 保持演示稳定且不伪报真实执行 | 延迟与更多故障注入 | P0 |
| Minecraft RCON | 尚未接入真实连接，当前明确暂缓 | 不进入当前 MVP；未来须经凭据管理、网络隔离和安全评审后单独立项 | 多实例、连接池、自动恢复 | P2 |
| 命令安全 | 已有少量白名单、换行/长度限制和审计 | 参数规则、超时、错误脱敏、权限测试 | 审批制高危命令、策略配置 | P0 |
| 通知 | 设置页包含 SMTP 演示表单，无后端持久化/发送 | MVP 可先提供站内状态；若接 SMTP，凭据只存后端 | 邮件、Webhook、企业微信等渠道 | P1/P2 |

### 5.7 自定义门户与组织配置

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| Manifest v1 | Schema、能力边界、示例文档和 Go 校验器已完成 | 已拒绝不兼容 Schema、非法路径、越权能力与主题注入 | 签名、市场、依赖声明 | P0 |
| 默认 MD3 门户 | 当前 Vue 门户可用 | 作为永远可用的安全回退 | 组织区块编排 | P0 |
| 加载与预览 | Admin 可导入/编辑、保存、预览和启用；公开端读取已校验配置，1.8 秒超时、404、错误类型或入口标记不符时回退 MD3，并记录当前会话故障 | 通用第三方门户按包规范完成实际部署演练 | 灰度、版本回滚、A/B 测试 | P0 |
| 默认门户恢复 | 支持 `?portal=md3` 临时回退；Admin 可用单次事务永久恢复默认 MD3，草稿与生效配置同步替换并写审计 | 自动回退与人工永久恢复均纳入发布检查表 | 历史版本回滚、恢复原因 | P0 |
| 组织基础设置 | 门户配置已持久化；组织名称/公开状态仍未形成设置 API | 名称、公开状态持久化并审计 | 自定义域名、品牌资源 | P1 |
| 扩展安全 | 公开能力白名单、同源入口、SRI 校验、CSP 和基础安全响应头已落地 | 自定义门户不能访问 Admin API、内部凭据和未发布数据；正常页与门户 404 均受安全头保护 | 独立沙箱域、签名包、供应链扫描 | P0 |

### 5.8 工程、部署与质量

| 功能 | 当前事实 | MVP 收口与验收 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| Docker Compose | Web/API/MySQL/Redis 可启动，MinIO/Swagger 使用 profile | 新机器按 README 一次启动，健康检查与端口覆盖可用 | 分环境 Compose、Kubernetes | P0 |
| OpenAPI / Apifox / Swagger | OpenAPI lint、58 条 Gin 路由、52 个前端请求和 13 个 Apifox 核心请求已自动对齐；核心集合与无凭据环境模板可直接导入 | OpenAPI 继续作为事实来源；核心集合在测试环境实跑 | 自动生成 SDK、Schema 级类型生成 | P0 |
| 数据迁移 | MySQL 迁移已有基础 | 新功能只通过迁移改表；升级和回滚说明明确 | 在线迁移、迁移审计 | P0 |
| Redis 缓存 | 公开数据缓存与部分失效已存在 | 发布、下线、项目公开变更后不会读到陈旧数据 | 分层缓存、热点保护 | P1 |
| 自动化测试 | 单命令质量门禁已覆盖契约、Go、前端构建、7 条 SPA 路由及 S1—S4 真实 Compose 集成套件，并已完整通过 | 补安全、备份恢复、性能和浏览器 E2E | 性能、故障演练、视觉回归 | P0 |
| 可观测性 | 有 `request_id` 与部分审计 | 统一结构化日志、关键写操作审计、健康检查 | 指标、追踪、告警 | P0/P1 |
| 安全基线 | RBAC、CORS、CSP、安全响应头、生产配置校验、IP/路由限流、上传双重大小/MIME 边界、命令注入测试及仓库/前端密钥扫描已落地 | 8/25 收口结构化日志脱敏与审计查询 | Redis 多实例限流、SAST/DAST、依赖签名 | P0 |
| 备份与恢复 | 尚未形成流程 | 至少提供 MySQL/媒体卷备份恢复脚本与演练记录 | 增量备份、异地恢复 | P1 |
| CI/CD | GitHub Actions 已执行 OpenAPI/路由/client/Apifox 对齐、Go test、前端类型检查与生产构建 | 后续部署流水线执行 Compose 集成和路由冒烟 | 自动部署、预览环境 | P1 |

### 5.9 AI 智能体（比赛版附加要求）

AI 是比赛要求，安排在 CMS 核心闭环与质量阶段完成之后，但必须进入 8 月 31 日比赛版本。它不作为聊天装饰，也不替代 RBAC、审批、发布状态机或 ServerAdapter。详细设计见 [AI 智能体集成设计](../architecture/ai-agent-integration.md)。

| 功能 | 当前事实 | 比赛版目标 | 扩展预留 | 优先级 |
| --- | --- | --- | --- | --- |
| 内容协作智能体 | 📐 已完成架构与安全边界设计 | 基于选定知识生成带引用的 Markdown，经人工确认后创建草稿 | 风格模板、多语言、批量整理 | P1（比赛必需） |
| 权限感知知识读取 | 📐 已定义组织/用户权限和引用要求 | 只读取当前用户可见资料，草稿保留来源，不跨组织泄露 | 混合检索、公开问答、反馈学习 | P1（比赛必需） |
| 组织运营观察员 | 📐 已定义只读分析方向 | 不进入比赛最小闭环 | 订阅、趋势预测、容量建议 | P2 |
| 智能体运行与工具平台 | 📐 已定义运行、工具调用、审批和审计模型 | 提供最小类型化工具、策略检查、运行记录和人工确认 | 可视化工作流、多智能体协作 | P1（比赛必需） |
| 模型供应商与治理 | 📐 已定义供应商中立接口 | 支持真实模型和明确标识的开发 Mock，控制超时、配额和数据外发 | 多模型路由、质量评测、私有化推理 | P1（比赛必需） |

## 6. 扩展路线

扩展项按价值和依赖分层，不按“看起来酷”决定先后。

### v0.2：运营与协作增强

- 内容详情、Markdown/基础富文本、修订历史和定时发布。
- 成员档案、批量邀请、邀请邮件和成员筛选。
- 项目任务看板、评论、附件和里程碑提醒。
- 知识库全文搜索、文章关联和导入/导出。
- 资源版本、下载统计、MinIO/S3 签名 URL。
- SMTP/Webhook 通知适配器及失败重试。
- Portal 公开知识问答和只读组织运营周报。
- 多模型路由、复杂工具审批和智能体工作流编排。

进入条件：`v0.1.0-mvp` 三条闭环稳定，权限与数据隔离测试通过，OpenAPI 不再频繁破坏性变更。

### v0.3：多组织与生态适配

- 真正的多组织切换、租户级配置、配额与数据隔离回归。
- 多服务器、多个 `ServerAdapter` 实例和统一状态聚合。
- 门户版本管理、灰度、回滚、自定义域名和可安装主题包。
- 组织级 Webhook、外部身份源和标准化数据导入。

进入条件：单组织部署完成真实使用验证；组织隔离、审计和配置模型经安全评审。

### v1.0：平台化能力

- 插件/适配器市场，但只接受声明式或受沙箱限制的扩展。
- 工作流编排、自定义表单、自定义角色和策略授权。
- 高可用部署、对象存储集群、异地备份和完整监控体系。
- 面向比赛与真实社团部署的版本治理、升级兼容和长期支持策略。

进入条件：扩展安全模型、版本兼容策略、运维责任和插件审核机制全部明确。不得以“执行任意第三方后端代码”作为捷径。

## 7. 依赖与开发顺序

| 能力 | 必须先稳定的依赖 | 原因 |
| --- | --- | --- |
| 内容/知识/资源公开 | 内容状态机、公开 DTO、缓存失效 | 防止草稿或内部字段泄露。 |
| 成员邀请 | 用户唯一性、成员关系、Token 生命周期、RBAC | 避免重复账户和越权加入。 |
| 项目成员/里程碑 UI | 项目权限、活跃成员列表、现有 CRUD API | 页面不能绕过服务端项目范围。 |
| 审批到服务器 | 申请持久化、审批事务、审计、ServerAdapter | 防止“批准成功但服务器未执行”的假成功。 |
| 自定义门户运行时 | Portal API 冻结、Manifest 校验、默认 MD3 回退 | 避免主题绑死内部接口或拖动核心契约。 |
| 通知 | 业务事件可靠持久化、密钥配置 | 通知失败不能导致业务状态回滚或密钥泄露。 |
| AI 智能体 | RBAC、内容状态机、知识可见性、审计、稳定 DTO | AI 的有效权限必须是用户权限、工具白名单、运行策略与人工确认的交集。 |

## 8. 取舍规则

1. P0 未通过阶段门时，不开始新的 P2 页面。
2. “接口已写”不等于完成；前端、服务端、契约、持久化和测试必须分别标记。
3. 外部依赖不可用时使用明确标识的适配器或降级状态，不伪造真实成功。
4. MVP 可减少编辑器、搜索、通知等体验增强，但不能削弱权限、审计、数据隔离和回退。
5. 新需求先归入本表的领域与版本；若会改变 P0 路径，必须同步调整 [schedule.md](../../schedule.md)。

## 9. 功能完成定义

一项功能只有同时满足以下条件，才可从 🟡/📐 改为 ✅：

- 用户入口和成功、空、失败、冲突/权限路径均有明确表现。
- OpenAPI、Go 实现和 TypeScript client 字段一致。
- 数据需要持久化时不依赖进程内临时状态或前端常量。
- 服务端执行真实授权，敏感字段不进入 Portal API。
- 至少有一条自动化测试，核心闭环另有 Compose 环境冒烟记录。
- README、功能表和相关规范已同步，不以 Mock 冒充生产能力。

## 10. 当前最先处理的错位

1. 为已完成的门户申请和审批闭环补齐 Apifox 用例；Compose 从零构建与运行态冒烟已通过。
2. 扩展成员邀请为批量邀请、撤销/重发、邮件通知和筛选。
3. 补服务器同步筛选、运维告警和 Mock 故障恢复演练；真实 RCON 明确延期，不阻断 MVP。
4. G5 通过后按 S6 完成比赛版 AI 内容闭环，先验证供应商抽象、权限交集、类型化工具、引用与审计，不直接开放发布等高风险写操作。
5. 继续为组织基础资料与通知设置增加真实 API；门户配置已接通，SMTP 示例密钥已移除，所有密钥只由服务端环境或受控密钥存储读取。
6. 建立三条核心闭环的测试，作为后续扩展进入条件。
