# remote is the safe default. mock is available only from `pnpm dev` when
# explicitly selected; production builds always call the real API.
VITE_API_MODE=remote
# 生产留空使用同源 /api；仅在本地前后端分端口调试时填写。
VITE_API_BASE_URL=
VITE_ORGANIZATION_SLUG=qutcraft
