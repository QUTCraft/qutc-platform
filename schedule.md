# QUTCraft CMS 项目排期

> 周期：2026 年 7 月 14 日 — 2026 年 8 月 31 日
>
> 项目主体：青岛理工大学 QUTCraft 社团技术部门
>
> 产品定位：面向校园社团与民间组织的可扩展门户与组织管理 CMS。QUTCraft Minecraft 社团官网是第一方门户实现，也是服务器联动能力的真实验证场景。

## 0. 项目目标与交付边界

### 产品目标

我们要做的不是一个“Minecraft 官网后台”，而是一套可以服务不同组织的数字化管理平台：

- **统一管理端**：Dashboard、用户与成员、项目、内容、资源、审核、权限。
- **可替换门户端**：默认提供 Material Design 3（MD3）门户；QUTCraft 提供 Minecraft 主题门户作为第一方风格实现。
- **组织数据沉淀**：门户、成员、项目、知识库和资源分发共享同一套组织模型。
- **服务器联动适配器**：RCON、白名单审批、在线状态属于可插拔的 Minecraft 适配能力，不写死在通用核心里。
- **比赛叙事**：以“组织数字化管理平台 + 可扩展门户 + 行业适配接口”为主线，以 QUTCraft 为真实落地案例。

### 8 月 31 日必须可演示的 MVP

1. 访客可以浏览一个完整的 MD3 组织门户。
2. 用户可以注册/登录，管理员可以进行 RBAC 权限控制。
3. 管理员可以管理公告、页面、资源、成员和项目。
4. 用户可以提交申请，管理员可以审核并留下审计记录。
5. 管理端可以展示 Dashboard，并完成一条资源发布或内容更新闭环。
6. QUTCraft 门户可以展示服务器信息、资源/项目和白名单入口。
7. 在配置了服务器适配器后，可以演示“白名单审核 → RCON 下发”的闭环；未配置时必须有明确的模拟模式。
8. 第三方可以按照门户 API 与 Manifest 规范创建自定义门户，默认回退到 MD3。

### 明确不在首期承诺的内容

- 不把多服务器集群、复杂工作流、在线编辑器、任意代码插件执行作为 8 月 31 日前的硬交付。
- 不以“部署 MinIO 集群”作为比赛亮点；首期只要求对象存储适配、上传鉴权和可替换实现。
- 不把 RCON 密码、数据库连接或管理权限暴露给浏览器。

## 1. 时间表

### 7 月 14 日 — 7 月 16 日：重新对齐与需求冻结

**目标：** 从“仓库刚建立”开始，建立一个所有成员都能执行的边界。

- 明确比赛版与 QUTCraft 社团版的共同核心、差异适配器和展示顺序。
- 冻结角色：访客、注册用户、成员、内容编辑、项目负责人、审核员、管理员。
- 冻结一级模块：门户、用户、成员、项目、知识库、资源、审核、后台、服务器适配器。
- 确定 MVP 用户故事、验收标准和演示脚本。
- 确认仓库分支、Issue/PR 约定、提交信息、环境变量和敏感信息规则。

**交付物：** 需求范围 v1、角色权限矩阵、MVP 验收清单、比赛叙事一页纸。

### 7 月 17 日 — 7 月 20 日：信息架构、原型与统一规范

**目标：** 在正式编码前先把系统“长什么样、怎么调用、谁能做什么”写清楚。

- 完成 MD3 通用门户、Minecraft QUTCraft 门户、管理端的页面线框。
- 确定门户路由、内容类型、资源类型、项目状态和审核状态。
- 建立统一前端规范、API 规范、错误码、分页、鉴权、审计和版本策略。
- 设计自定义门户 Manifest、主题 Token、资源加载和 API 能力边界。
- 根据技术栈确认工程入口：Vue 3 + Vite + TypeScript + Element Plus；Go + Gin；GORM；MySQL；Redis；MinIO；Docker Compose；Nginx；JWT + RBAC；Swagger。

**交付物：** HTML 页面演示稿、`docs/architecture/platform-standard.md`、API 草案、页面/路由清单。

### 7 月 21 日 — 7 月 27 日：工程骨架与身份权限底座

**目标：** 让前后端、数据库和部署环境可以稳定启动。

- 初始化 Vue 与 Go 工程，补充 lint、format、测试和环境配置模板。
- 建立 MySQL 基础迁移：组织、用户、角色、权限、成员关系、审计日志。
- 实现注册、登录、刷新、退出和当前用户接口。
- 实现 JWT 校验、RBAC 中间件、前端路由守卫和统一错误处理。
- 完成 Docker Compose 的开发环境：API、Web、MySQL、Redis；MinIO 先以可选服务接入。
- Swagger 生成第一版接口文档。

**阶段门：** 新成员可以按 README 启动项目；未登录不能访问管理端；权限拒绝有统一响应。

### 7 月 28 日 — 8 月 3 日：门户与 CMS 内容闭环

**目标：** 先让一个非 Minecraft 组织能够使用平台。

- 完成门户首页、公告/文章、组织介绍、项目列表、资源列表、知识库入口。
- 实现后台页面/文章的创建、编辑、草稿、发布、下线。
- 实现媒体资源上传、元数据、引用关系和下载鉴权。
- 完成前端内容管理页面，优先使用 MD3 设计系统。
- 建立公开 API 与管理 API 的权限边界。

**阶段门：** 管理员发布一篇内容后，访客门户可以看到；资源可以上传、展示、下载。

### 8 月 4 日 — 8 月 10 日：用户、成员、项目与知识库

**目标：** 形成组织数字化管理的核心产品价值。

- 完成用户资料、成员档案、成员角色、加入/退出记录。
- 完成项目创建、负责人、成员、状态、里程碑和公开展示。
- 完成知识库分类、文章、目录、草稿与发布。
- 完成后台用户管理、成员管理、项目管理页面。
- 接入 Redis 缓存首页、公开内容列表和平台配置；缓存失效规则必须写入规范。

**阶段门：** 用一个虚拟校园社团数据集跑通“成员 — 项目 — 知识库 — 门户展示”。

### 8 月 11 日 — 8 月 17 日：QUTCraft 服务器适配与审批闭环

**目标：** 把 QUTCraft 的特色能力做成适配器，不污染通用核心。

- 完成白名单申请、补充材料、审核、驳回和审计记录。
- 抽象 `ServerAdapter` 接口，定义连接检查、执行命令、白名单变更和状态查询能力。
- 实现 Minecraft RCON 适配器；RCON 凭据只允许服务端读取。
- 完成“审核通过 → 写入白名单 → 返回结果”的可重试流程。
- 实现服务器状态卡片：在线状态、在线人数、版本、最近同步时间；TPS/内存等高级指标作为可选字段。
- 提供无服务器环境下的 Mock Adapter，保证比赛演示可重复。

**阶段门：** 在 Mock 模式和真实测试服各跑通一次审批闭环；失败时不会误报成功。

### 8 月 18 日 — 8 月 23 日：自定义门户与双风格演示

**目标：** 证明系统既有统一平台能力，也允许组织保留自己的文化表达。

- 完成默认 MD3 Portal 的 Manifest 加载、导航、主题 Token 和内容 API 绑定。
- 完成 QUTCraft Minecraft Portal 的第一方实现，验证同一 API 下的不同视觉层。
- 实现门户版本、启用/停用、预览和回退到默认 MD3。
- 限制自定义门户能力：只能调用公开 Portal API，不能访问管理 API、RCON 或内部凭据。
- 补充门户开发示例、Manifest 示例和兼容性检查清单。

**阶段门：** 切换 Portal 配置后可以在不修改核心业务代码的情况下切换门户外观。

### 8 月 24 日 — 8 月 27 日：集成、部署、安全与质量

**目标：** 从“能跑”推进到“可稳定演示、可交接”。

- 整合前端、API、数据库、缓存、对象存储和 Nginx 配置。
- 完成 Docker Compose 一键启动和生产环境变量说明。
- 执行 API 单测、关键流程集成测试、前端冒烟测试和权限测试。
- 检查 SQL 注入、越权、上传类型、下载鉴权、CORS、CSP、日志脱敏和限流。
- 进行一次从注册到门户发布、从白名单申请到 RCON/Mock 下发的全链路演练。
- 修复阻断级问题，冻结 MVP 功能范围。

**阶段门：** 新机器可以按部署文档启动；关键演示流程连续运行三次成功。

### 8 月 28 日 — 8 月 31 日：文档、答辩与交付冻结

**目标：** 把工程成果转化为评委能够理解、团队能够复现的交付物。

- 完成系统架构图、模块图、数据流图、权限模型和自定义门户扩展图。
- 完成 README、部署手册、API 文档、门户开发指南和演示数据说明。
- 录制比赛版平台演示：组织门户 → 管理端发布 → 资源分发 → 审批 → 适配器联动。
- 录制 QUTCraft 专属演示：Minecraft 风格门户 → 白名单申请 → 审核 → 服务器联动。
- 完成答辩 PPT、产品价值、技术亮点、风险与后续计划。
- 代码冻结，建立 `v0.1.0-mvp` 标签，记录未完成事项和赛后路线图。

**最终交付：** 可启动仓库、可复现部署、双门户演示、API/门户规范、比赛材料和已知问题清单。

## 2. 每周协作节奏

- 每周一：确认本周目标、拆解 Issue、标记依赖和风险。
- 每周三：15 分钟进度同步，只讨论阻塞项和接口变更。
- 每周六：合并可演示版本，更新演示数据和阶段记录。
- 所有跨模块变更必须先更新规范或 Issue，再改代码。
- 所有涉及权限、资源上传、RCON、数据库迁移的 PR 必须至少一人复核。

## 3. 风险与取舍

| 风险 | 触发信号 | 处理方式 |
| --- | --- | --- |
| RCON 测试服不可用 | 8 月 11 日前没有稳定测试环境 | 使用 Mock Adapter，不阻塞比赛版演示 |
| 自定义门户拖慢核心开发 | Portal API 仍频繁变更 | 先冻结 Manifest v1，只支持公开内容与资源读取 |
| 内容编辑器范围膨胀 | 开始讨论多人协同、复杂排版 | MVP 只支持基础富文本与媒体引用 |
| 部署环境不一致 | 成员本地能跑、演示机不能跑 | 优先 Docker Compose 与 `.env.example`，不手工配置 |
| 权限设计返工 | 出现“前端隐藏按钮即代表无权限” | 以服务端 RBAC 为准，前端只做体验层拦截 |

## 4. 完成定义

一个功能只有同时满足以下条件，才算完成：

1. 有明确的用户故事、API 契约和权限要求。
2. 有可运行实现和至少一条成功/失败路径。
3. 有前端空状态、加载态、错误态和权限拒绝态。
4. 通过对应测试，并在 Docker Compose 环境验证。
5. 文档、Swagger 或门户开发示例已同步。
