# QUTCraft Commons 开发排期 v2

> 项目周期：2026 年 7 月 14 日 — 2026 年 8 月 31 日
>
> 重排基线：2026 年 7 月 25 日
>
> 项目主体：青岛理工大学 QUTCraft 社团技术部门
>
> 目标版本：`v0.1.0-mvp`
>
> 功能范围与实时完成度见：[功能地图 v2](docs/product/feature-map-v2.md)

## 1. 排期原则

本排期不再按“做完一个页面”计算进度，而以可验证的业务切片推进。每个切片至少包含界面、OpenAPI、Go 实现、持久化、权限、错误处理和测试中的相关部分。

产品由三个边界清晰的部分组成：

- **Portal**：面向访客的宣传官网，只消费已发布的公开数据；它不承担管理、审批或组织内部协作。
- **Admin**：独立的内容生产与组织管理工作台，所有写操作由服务端 RBAC 约束。
- **Adapter**：将通用平台与 Minecraft、SMTP、对象存储等外部系统连接；外部依赖失败不能污染核心业务状态。

开发顺序遵循以下规则：

1. 先修复已有界面、API 与契约之间的错位，再增加新模块。
2. P0 阶段门没有通过时，不投入 P2 扩展。
3. Mock 必须明确标识，不得在产品或演示中伪装成真实 RCON、邮件或对象存储执行结果。
4. 任何功能都不能只靠前端隐藏实现权限控制。
5. 每个阶段末保留可运行版本；尚未完成的增强项按“切线”延期，不拖垮核心闭环。

## 2. 8 月 31 日的交付目标

### 2.1 必须完成的三条闭环

| 闭环 | 演示路径 | 通过标准 |
| --- | --- | --- |
| 内容闭环 | Admin 创建/编辑 → 发布 → Portal 展示 → Admin 下线 | 连续三次成功；Portal 不可读取草稿和内部字段；缓存及时失效。 |
| 申请闭环 | Portal 提交 → Admin 审批 → 审计 → Mock/真实 ServerAdapter | 申请真实持久化；重复审批返回冲突；适配器失败不误报业务成功。 |
| 门户闭环 | Admin 选择 Manifest → 加载自定义门户 → 异常时回退 MD3 | 不修改核心业务代码即可切换；自定义门户只访问 Portal API。 |

### 2.2 MVP 能力边界

必须交付：身份/RBAC、内容发布、资源基础上传下载、成员邀请与管理、项目及里程碑基础协作、知识目录、申请审批、Mock ServerAdapter、自定义门户加载回退、Compose 部署、契约和关键测试。

允许延期：复杂富文本、全文搜索、项目看板、多级审核、多组织 SaaS、插件市场、多服务器集群、2FA、完整通知中心和高可用部署。

## 3. 截至 7 月 25 日的真实基线

### 3.1 已经具备

- Vue 3 + Vite + TypeScript + Element Plus 门户与独立管理端。
- Go + Gin + GORM API、MySQL 迁移、Redis 公开缓存、JWT、RBAC 和基础审计。
- 注册、登录、刷新、退出、当前用户与成员关系基础接口。
- 门户首页、动态、项目、资源、知识库、申请表单和公开服务器状态页面。
- 内容草稿、编辑、发布、下线以及门户读取的基础闭环。
- 项目 CRUD，以及项目成员/里程碑的 Go API。
- 资源基础上传/下载接口。
- 管理端申请审核和受限服务器命令的演示实现。
- Docker Compose 的 Web、API、MySQL、Redis；MinIO 和 Swagger 可选 profile。
- OpenAPI、Apifox/Swagger 使用文档、统一规范和 Portal Manifest v1 设计。
- 门户响应式布局与按季节、昼夜自动变化的主题。

### 3.2 需要优先纠正的错位

| 编号 | 问题 | 影响 | 处理阶段 |
| --- | --- | --- | --- |
| G-01 | 申请页面调用 Portal 提交接口，后端路由、OpenAPI 与落库已补齐 | 仍需 Compose/Apifox 验证真实联调 | S0 已完成，S3 继续闭环 |
| G-02 | “邀请成员”按钮是占位，没有事件和后端接口 | 成员生命周期不完整 | S2 |
| G-03 | 项目成员与里程碑后端接口已有，管理页面未接入 | 功能被隐藏，无法实际使用 | S2 |
| G-04 | 申请列表与审批状态已迁移数据库；适配器执行仍是 Mock | 需补 ServerAdapter 结果持久化与集成测试 | S0 部分完成，S3 |
| G-05 | 设置页只显示本地演示成功，SMTP 示例密钥位于前端常量 | 配置不可用且容易误导安全边界 | S4/S5 |
| G-06 | 服务器命令目前是带白名单的 Mock，尚无 `ServerAdapter` 层 | 无法安全替换为真实 RCON | S3 |
| G-07 | Manifest 只有规范和静态样例，没有加载/预览/回退运行时 | 自定义门户卖点尚未闭环 | S4 |
| G-08 | 自动化测试主要集中在认证服务 | 回归风险随模块增加迅速上升 | 全阶段，S5 收口 |

### 3.3 本次执行进展

本轮已完成 G-01 的第一阶段收口：门户申请现在有正式 OpenAPI、Go POST 路由、数据库实体、组织隔离、重复待审冲突和后台数据库列表；审批状态改为数据库事务更新并写入审计。申请到服务器的 `ServerAdapter` 同步仍属于 S3，尚未被本轮实现替代。

本轮验证结果：Go 全量测试通过，前端类型检查和生产构建通过。由于 Docker 当前未运行，Compose 冒烟与真实接口联调待 Docker 恢复后执行。

## 4. 执行时间表

### S0 · 7 月 25 日 — 7 月 27 日：基线校准与工程止血

**目标：** 让仓库文档、契约和实际运行状态一致，为后续开发建立可测基线。

| 日期 | 主要任务 | 可交付结果 |
| --- | --- | --- |
| 7/25 | 建立功能地图 v2、重排 schedule、标出页面/API/持久化错位 | 功能状态、优先级、依赖和扩展版本可查。 |
| 7/26 | 用 Compose 跑 Portal/Admin/Auth/CMS 冒烟；记录真实失败；检查 OpenAPI 与已注册 Go 路由 | 一份按 P0/P1 排序的问题清单；远程模式不靠 Mock 掩盖失败。 |
| 7/27 | 修复阻断级契约错位；至少为申请提交定义正式 OpenAPI 和数据库方案；补开发检查脚本 | 后续实现不再凭前端类型猜接口。 |

**阶段门 G0：**

- Web、API、MySQL、Redis 能按 README 启动。
- 未登录访问 Admin 被阻止，权限失败使用统一响应。
- Portal 核心页面在远程 API 模式有明确加载、空、错状态。
- G-01 至 G-08 都有归属阶段，不存在“默认算完成”的功能。

### S1 · 7 月 28 日 — 8 月 2 日：内容、资源与知识闭环

**目标：** 把已存在的 CMS 能力从“能操作”收口成稳定的公开内容生产线。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 7/28 | 对齐内容 DTO、状态机和权限；补创建/编辑/发布/下线服务测试 | 非法状态被拒绝；无发布权限无法发布。 |
| 7/29 | 完成 Portal 内容完整阅读路径、统一 404/空错态；确认动态与项目领域分离 | 发布内容可完整消费；项目不会混入动态列表。 |
| 7/30 | 完成资源元数据、上传类型/大小限制、下载授权和内容引用 | Portal 不获得对象存储凭据或长期内部地址。 |
| 7/31 | 接入知识目录管理界面；文章与目录关联；修复目录缓存失效 | 后台可维护目录，门户只显示公开文章。 |
| 8/1 | 覆盖发布、下线、资源下载、缓存失效的集成用例 | 内容变更后 Portal 不读取陈旧数据。 |
| 8/2 | Compose 全链路演练、缺陷修复和阶段演示 | 内容闭环连续三次成功。 |

**阶段门 G1：**

- 后台发布后 Portal 可见，下线后不可见。
- 草稿、作者内部字段和对象键不进入 Portal API。
- 资源成功/失败下载、知识空目录和缓存失效均有可验证路径。

**本阶段切线：** 定时发布、复杂富文本、全文搜索、修订历史进入 `v0.2`。

### S2 · 8 月 3 日 — 8 月 8 日：成员与项目协作

**目标：** 补齐组织数字化管理的核心协作能力，不扩展成完整项目管理软件。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/3 | 设计邀请模型、一次性 Token、过期/重复策略和 OpenAPI | 邀请不会创建重复成员，Token 不明文持久化。 |
| 8/4 | 实现邀请创建、查询/接受与成员关系落库；可选邮件仅做适配器 | 无 SMTP 时仍可复制邀请链接完成流程。 |
| 8/5 | 接通 Admin “邀请成员”；补角色修改、停用和 Owner 保护 | 原占位按钮成为真实操作，越权写入被拒绝。 |
| 8/6 | 在项目管理中接入成员列表、添加、改角色和移除 | 只能添加当前组织活跃成员；Owner 不可移除。 |
| 8/7 | 接入里程碑创建、编辑、完成、删除及日期校验 | 里程碑计数与列表一致，错误状态可见。 |
| 8/8 | 用虚拟社团数据演练“邀请 → 加入 → 分配项目 → 完成里程碑” | 主路径与权限失败路径均通过。 |

**阶段门 G2：**

- 邀请成员按钮可完成真实邀请流程。
- 成员停用后不能继续使用受保护能力。
- 项目成员和里程碑不再是“只有接口、没有产品入口”。

**本阶段切线：** 成员技能档案、项目任务看板、评论、工时和甘特图进入 `v0.2`。

### S3 · 8 月 9 日 — 8 月 15 日：申请审批与 Minecraft 适配

**目标：** 完成 QUTCraft 特色闭环，同时保持通用核心和游戏服务器解耦。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/9 | 增加申请数据库模型、迁移、Portal 提交接口和校验 | Portal 提交后后台立即可见，重启不丢失。 |
| 8/10 | 申请分页、组织隔离、详情和审核原因；清除内存演示状态 | 不同组织数据不可串读；演示数据可由 seed 创建。 |
| 8/11 | 审批事务、并发冲突、审批审计和幂等语义 | 同一申请不能被重复决定；返回 `409`。 |
| 8/12 | 抽象 `ServerAdapter`：状态、受限命令、白名单增删与健康检查 | 业务层不直接依赖 RCON 客户端。 |
| 8/13 | 实现可重复 Mock Adapter；分离“审批结果”和“服务器同步结果” | 外部失败时不把未执行命令记录成成功。 |
| 8/14 | 在测试服可用时接真实 RCON；补超时、参数白名单、日志脱敏 | 凭据只在服务端；任意命令不能由浏览器透传。 |
| 8/15 | 连续演练申请、批准/拒绝、Mock/真实同步和失败重试 | Mock 连续三次成功；真实 RCON 至少一次或形成明确阻塞记录。 |

**阶段门 G3：**

- 申请闭环使用真实数据库，不依赖进程内状态。
- 审批人、原因、原/新状态、`request_id` 和适配器结果可追溯。
- 没有测试服务器时，Mock 明确显示为模拟模式且不阻断比赛演示。

**本阶段切线：** 补充材料、批量审批、多服务器和高危命令工作流进入后续版本。

### S4 · 8 月 16 日 — 8 月 21 日：自定义门户运行时

**目标：** 把 Portal Manifest 从文档变成可演示的产品能力。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/16 | 冻结 Manifest v1 与 Portal API 能力清单；实现 Schema 校验 | 未声明能力、错误版本和非法 URL 被拒绝。 |
| 8/17 | 建立门户配置持久化 API、RBAC 与变更审计 | 只有授权管理员可以保存/启用门户。 |
| 8/18 | 实现 Admin 预览和启用流程，替换设置页的本地“假保存” | 刷新后配置仍在；预览不影响当前线上门户。 |
| 8/19 | 实现运行时加载、超时、资源错误和默认 MD3 回退 | 自定义入口失败时网站仍可访问。 |
| 8/20 | 将 Minecraft 第一方门户接入同一 Portal API 和 Token 契约 | 不复制 Admin 业务逻辑，不访问内部 API。 |
| 8/21 | 安全边界、CORS/CSP 和兼容检查；双门户演示 | 切换主题不改变 Admin URL、权限或数据。 |

**阶段门 G4：**

- 默认 MD3 永远可回退。
- Minecraft 门户和 MD3 使用同一公开数据契约。
- 自定义门户无法读取草稿、成员邮箱、审批、RCON 和服务端密钥。

**本阶段切线：** 主题市场、在线代码编辑、任意脚本插件、A/B 测试进入 `v0.3+`。

### S5 · 8 月 22 日 — 8 月 26 日：质量、安全与可部署性

**目标：** 从“功能可演示”推进到“可复现、可排错、可交接”。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/22 | OpenAPI lint、Go 路由差异检查、Apifox 核心集合 | 文档、服务端和前端 client 不再字段漂移。 |
| 8/23 | Go 单测、关键服务集成测试、前端构建和路由冒烟 | 三条核心闭环进入自动化回归。 |
| 8/24 | 越权、CORS、限流、上传、命令注入、日志脱敏和密钥扫描 | P0 安全问题清零；敏感配置不进入浏览器包。 |
| 8/25 | 结构化日志、健康检查、审计查询；MySQL/媒体备份恢复演练 | 问题可用 `request_id` 定位，备份可恢复。 |
| 8/26 | 新机器 Compose 部署、端口覆盖、故障降级与三次全链路回归 | 不依赖开发者机器上的隐式配置。 |

**阶段门 G5：**

- `go test`、前端构建、OpenAPI 校验和核心冒烟全部通过。
- Compose 环境中三条核心闭环连续三次成功。
- 仓库不含生产密码、真实成员隐私、RCON/SMTP/MinIO 密钥。
- 阻断级和高风险缺陷清零，其他问题进入已知问题清单。

### S6 · 8 月 27 日 — 8 月 31 日：交付冻结与比赛材料

**目标：** 把可运行系统整理成评委能理解、团队能复现的正式交付。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/27 | 功能冻结、生成演示组织与 QUTCraft 两套 seed 数据 | 演示不依赖手工改数据库。 |
| 8/28 | 更新 README、部署、API、门户开发、备份恢复和已知问题文档 | 新成员可以独立启动和找到入口。 |
| 8/29 | 完成架构图、权限/数据流、适配器与扩展路线图 | 比赛叙事与实际实现一致。 |
| 8/30 | 录制通用平台和 QUTCraft 两套演示；准备答辩 PPT 与应急视频 | 演示脚本在离线/外部服务失败时仍可运行。 |
| 8/31 | 最终回归、代码冻结、发布说明和 `v0.1.0-mvp` 标签 | 仓库、文档、演示材料和已知问题完整归档。 |

**最终阶段门 G6：**

- 发布标签只包含通过 G5 的代码。
- 通用组织演示突出数字化管理，QUTCraft 演示突出 Minecraft 门户和服务器适配。
- 演示中明确区分“真实执行”“Mock”“规划功能”。

## 5. 扩展功能如何进入排期

新增需求先登记到 [功能地图 v2](docs/product/feature-map-v2.md)，再按以下规则进入迭代：

| 类型 | 进入条件 | 安排方式 |
| --- | --- | --- |
| P0 缺陷 | 阻断核心闭环、安全边界或数据正确性 | 立即进入当前阶段，必要时挤出同阶段 P1。 |
| P1 增强 | 当前阶段门有明确帮助，且预计不改变核心契约 | 放入阶段预留时间；未完成进入下一阶段首日评估。 |
| P2 扩展 | 依赖稳定且不影响 `v0.1.0-mvp` | 只做设计/接口预留，开发进入 `v0.2+`。 |
| 外部集成 | 有测试环境、凭据管理和失败降级方案 | 以 Adapter 接入，不允许绑死核心服务。 |

每个扩展需求至少写明：目标用户、价值、依赖、权限、公开/内部数据边界、验收方式、失败降级和计划版本。缺少这些信息的需求不进入开发中状态。

## 6. 延期切线与保底方案

如果进度落后，按下列顺序收缩，不削弱安全和数据正确性：

1. 保留纯文本/Markdown，延期复杂富文本和协同编辑。
2. 保留基础资源上传下载，MinIO 使用可替换 profile；延期分片、版本和统计。
3. 保留站内可见的申请状态，延期 SMTP 真实发送；绝不在前端保存邮件密码。
4. 保留 Mock ServerAdapter 并明确标识，延期真实 RCON；绝不直连或放宽命令白名单赶进度。
5. 保留一个第一方 Minecraft Manifest 和默认 MD3 回退，延期主题市场、在线编辑器和灰度发布。
6. 保留项目成员与里程碑，延期任务看板、评论、甘特图和工时。
7. 保留单组织完整隔离模型，延期面向公网的多租户运营能力。

三条核心闭环、RBAC、审计、公开数据边界、持久化和回退机制不在可裁剪范围内。

## 7. 协作与检查节奏

### 每日

- 开始开发前确认当天切片的 OpenAPI、权限和完成定义。
- 结束前更新对应 Issue/功能状态，记录已验证命令和未解决风险。
- 不把仅在 Mock API 模式可用的页面计入远程环境完成度。

### 每个阶段

- 阶段开始：只拉取该阶段 P0/P1，标记责任人与依赖。
- 阶段中点：运行一次 Compose 集成，不等到最后一天集中联调。
- 阶段结束：按阶段门逐项验收；未通过的 P0 自动进入下一阶段首位。

### 代码与契约

- 跨端接口先改 OpenAPI，再改 Go 与 TypeScript client。
- 涉及权限、迁移、上传、服务器适配和门户配置的变更必须复核。
- 合并前至少运行与改动相关的格式检查、测试、构建和契约校验。
- 任何密钥只通过后端环境或受控密钥系统提供，不进入 `VITE_*`、前端常量或 Git。

## 8. 进度报告模板

每次阶段同步只回答以下问题，避免用页面数量代替真实进度：

```text
阶段 / 日期：
本次完成的业务切片：
对应功能地图编号或领域：
已验证：页面 / OpenAPI / Go / 数据库 / RBAC / 测试
仍缺失：
阻塞与外部依赖：
是否影响阶段门：是 / 否
需要延期到的版本：
```

## 9. 统一完成定义

一个功能只有满足以下条件才能计入 schedule 完成度：

1. 用户成功、空、失败、冲突和权限路径有明确表现。
2. OpenAPI、Go API、前端 client 和持久化模型一致。
3. 权限由服务端执行，Portal 不泄露内部字段。
4. 关键状态不会因进程重启丢失；Mock 有明确标识。
5. 至少有一条自动化验证，核心闭环在 Compose 中通过。
6. 相关 README、功能地图、Swagger/Apifox 和变更说明已同步。
