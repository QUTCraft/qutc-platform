# QUTCraft Commons 开发排期 v2

> 项目周期：2026 年 7 月 14 日 — 2026 年 8 月 31 日
>
> 重排基线：2026 年 8 月 5 日
>
> 项目主体：青岛理工大学 QUTCraft 社团技术部门
>
> 目标版本：核心基线 `v0.1.0-mvp`；最终比赛版 `v0.1.0-competition`
>
> 功能范围与实时完成度见：[功能地图 v2](docs/product/feature-map-v2.md)；延期项评估见：[延期功能评估](docs/product/deferred-work.md)

## 1. 排期原则

本排期不再按“做完一个页面”计算进度，而以可验证的业务切片推进。每个切片至少包含界面、OpenAPI、Go 实现、持久化、权限、错误处理和测试中的相关部分。

产品由四个边界清晰的部分组成：

- **Portal**：面向访客的宣传官网，只消费已发布的公开数据；它不承担管理、审批或组织内部协作。
- **Admin**：独立的内容生产与组织管理工作台，所有写操作由服务端 RBAC 约束。
- **Commons Agent**：比赛主服务；读取显式授权的组织知识、生成带引用方案并提出类型化操作，所有写入必须人工批准。
- **Integration**：将通用平台与 SMTP、对象存储和模型服务等外部系统连接；外部依赖失败不能污染核心业务状态。

开发顺序遵循以下规则：

1. 先修复已有界面、API 与契约之间的错位，再增加新模块。
2. P0 阶段门没有通过时，不投入 P2 扩展。
3. Mock 必须明确标识，不得在产品或演示中伪装成真实邮件、模型或对象存储执行结果。
4. 任何功能都不能只靠前端隐藏实现权限控制。
5. 每个阶段末保留可运行版本；尚未完成的增强项按“切线”延期，不拖垮核心闭环。

## 2. 8 月 31 日的交付目标

### 2.1 必须完成的四条闭环

| 闭环 | 演示路径 | 通过标准 |
| --- | --- | --- |
| 内容闭环 | Admin 创建/编辑 → 发布 → Portal 展示 → Admin 下线 | 连续三次成功；Portal 不可读取草稿和内部字段；缓存及时失效。 |
| 申请闭环 | Portal 提交 → Admin 审批 → 审计 → 通知 Outbox | 申请真实持久化；重复审批返回冲突；通知失败不回滚审批事实。 |
| 门户闭环 | Admin 选择 Manifest → 加载自定义门户 → 异常时回退 MD3 | 不修改核心业务代码即可切换；自定义门户只访问 Portal API。 |
| AI 活动服务闭环 | 活动需求 → 组织知识 → 带引用方案 → 人工批准 → 项目/里程碑/公告草稿 → 人工发布 | 真实模型可演示；AI 不自动发布；建议操作按 Schema 校验、事务执行并逐项审计。 |

### 2.2 MVP 能力边界

必须交付：身份/RBAC、内容发布、资源基础上传下载、成员邀请与管理、项目及里程碑基础协作、知识目录、申请审批与通知、自定义门户加载回退、Compose 部署、契约和关键测试。

服务创新类比赛主线：交付“结构化活动需求 → 选择组织知识 → AI 生成带引用的活动方案 → 人工逐项批准 → 创建项目、里程碑与 CMS 草稿 → 人工发布与复盘”的校园组织服务闭环。CMS 和项目能力作为智能体安全执行的底座，不再把 AI 表述为附加写作按钮。

允许延期：复杂富文本、全文搜索、项目看板、多级审核、多组织 SaaS、插件市场、多服务器集群、2FA、多渠道通知中心和高可用部署；基础审批通知 Outbox、内容修订快照、下载统计、邀请模板、默认组织偏好和单机 AI 队列已补齐首版。

## 3. 截至 8 月 5 日的真实基线

### 3.1 已经具备

- Vue 3 + Vite + TypeScript + Element Plus 门户与独立管理端。
- Go + Gin + GORM API、MySQL 迁移、Redis 公开缓存、JWT、RBAC 和基础审计。
- 注册、登录、刷新、退出、当前用户与成员关系基础接口。
- 门户首页、动态、项目、资源、知识库、申请表单和公开服务器状态页面。
- 内容草稿、编辑、发布、下线以及门户读取的基础闭环。
- 项目 CRUD，以及项目成员/里程碑的 Go API。
- 资源基础上传/下载接口。
- 管理端申请审核和受限服务器命令的演示实现。
- Docker Compose 的 Web、API、MySQL、Redis；MinIO 和 Swagger 可选 profile。
- OpenAPI、Apifox/Swagger 使用文档、统一规范和 Portal Manifest v1 设计。
- 门户响应式布局与按季节、昼夜自动变化的主题。

### 3.2 需要优先纠正的错位

| 编号 | 问题 | 影响 | 处理阶段 |
| --- | --- | --- | --- |
| G-01 | ✅ Portal 申请、OpenAPI、Go、落库、重复冲突、Admin 审批、通知 Outbox 和 Compose 联调均已完成 | 后续只做多渠道通知、补充材料与申请人自助查询 | S3 已完成 |
| G-02 | 成员单个/批量邀请、注册接受、过期/重复冲突、Owner 保护、主动撤销、可选 SMTP 投递和组织级邀请模板已接入 | 多语言模板、成员档案仍延期 | S2 已完成首版 |
| G-03 | ✅ 项目成员、角色、里程碑、权限回归和 Compose 主路径已接入 | 批量分配和项目详情进入后续增强 | S2 已完成 |
| G-04 | ✅ 申请、审批事务、审计、通知 Outbox 与 S3 集成测试均已完成 | 补充材料和批量审批延期 | S3 已完成 |
| G-05 | 门户设置、邮件状态、邀请模板和申请审批通知 Outbox 已接真实 API；邮件凭据改由后端环境变量管理，投递结果可追踪/重试 | 多实例 worker、Webhook/企业微信仍延期 | S5 已完成首版 |
| G-06 | ✅ 游戏服务器远程控制已从产品、API、前端与运行时中移除，审批保持纯业务事务 | 无 | 2026-08-17 收口 |
| G-07 | Manifest 校验、持久化、Admin 预览/启用、公开加载、错误回退与永久恢复均已完成 | 自定义门户运行时已闭环 | S4 已完成 |
| G-08 | ✅ 契约、Go、S1—S6 Compose、备份恢复和 Playwright 桌面/移动关键流程已形成门禁 | 后续补性能与视觉回归 | S5 已完成 |

### 3.3 本次执行进展

G-01 与 S3 已完成：门户申请具备正式 OpenAPI、Go 路由、数据库实体、组织隔离、重复待审冲突和后台列表；审批决定、审计和通知 Outbox 在事务中建立，通知失败不回滚审批事实。通用组织申请表已按组织 slug 去除 Minecraft 语义。

本轮验证结果：Go 全量测试通过，前端类型检查和生产构建通过。Docker 恢复后已补跑 Compose 冒烟与真实接口联调。

### 3.4 7 月 26 日执行记录

- 已修复 OpenAPI 中“创建内容草稿”误挂知识目录详情路径、漏记 `/healthz` 的契约错位。
- 新增 `scripts/check-openapi-routes.py` 时覆盖 49 条 Gin 路由；门禁随功能扩展持续维护，当前检查 92 条路由、唯一 `operationId`、路径参数声明和 YAML 重复键并全部通过。
- Docker Desktop 已启动；重拉 `gcr.io/distroless/static-debian12:nonroot` 成功，随后 `docker compose up -d --build` 完整通过。
- 重建后的 Web、API、MySQL、Redis 均正常；健康检查、Portal 公开读取、登录、Admin 会话/Dashboard/CMS/申请列表、无令牌 `401` 均通过。
- Portal 申请真实联调通过：首次提交 `201`，重复待审提交 `409`，测试申请已清理。
- S1 内容闭环已提前完成第一轮：内容状态机、Portal 正文详情、详情路由、资源列表真实元数据、后台资源上传、受控公开下载和 Admin 知识目录页面已接入。
- 资源卷补充 `media-init` 权限初始化服务，保持 API 使用 nonroot 镜像的同时允许 Compose 上传媒体文件；上传成功后主动失效 Portal 缓存。
- Compose 内容联调通过：临时内容创建 `201`、草稿详情 `404`、发布 `200`/详情 `200`、下线 `200`/详情 `404`；资源上传 `201`、公开下载 `200`、下线后下载 `404`，测试数据已清理。
- S2 成员邀请已提前完成首轮：新建邀请、公开预览、默认 7 天/最长 30 天过期、同邮箱重复冲突、已有账户接受和注册携带 token 两条路径已实现；token 只存哈希，非 Owner 不能授予 owner。
- Compose 邀请联调通过：创建邀请、预览 `200`、重复创建 `409`、携带 token 注册 `201`、editor 越权读取成员目录 `403`、token 使用后预览 `410`，测试账户和邀请已清理。
- 项目协作入口已提前接入：后台项目列表新增“成员”和“里程碑”操作抽屉；成员支持添加、角色修改、移除，里程碑支持创建、编辑、完成、删除和日期选择；负责人保护由服务端与界面共同约束。
- 项目协作真实接口联调通过：活跃成员添加/改角色/移除、里程碑创建/完成/删除均成功，临时测试用户、里程碑和成员关系已清理；项目成员与里程碑计数刷新正常。
- S2 主路径 Compose 演练通过：邀请创建 → 携带 token 注册 → 已使用 token 再访问返回 `410` → 新成员分配项目 → 里程碑完成并写入 `completed_at`；临时邀请、用户、成员关系和里程碑均已清理。
- 7 月 27 日问题修复：GitHub #5 将桌面端 Admin 侧栏改为固定视口布局、移动端保持抽屉导航；GitHub #1 在 API 客户端统一处理 `401`，刷新令牌成功则重试，刷新失败则清理会话并返回登录页。
- S1 首片提前收口：新增统一 `ContentInput` DTO、内容字段标准化与状态机服务；创建/编辑共用校验规则，发布/下线状态转换补齐独立服务测试。
- Docker Desktop 端口校准：确认宿主机 `5173` 位于保留端口范围 `5154–5253`，Compose/Web/Vite 默认端口统一调整为 `8082`，并完成 Web/API 冒烟验证。
- S1 阅读路径继续收口：详情页跟随路由参数变化重新加载；404、已下线内容和路由类型不匹配统一回到对应 Portal 列表；补充公开响应字段边界测试，动态仍只读取 `news` 内容，项目仍独立读取公开 `Project` 数据。
- S1 资源安全边界收口：上传类型改为服务端文件签名识别，保留 10 MiB 大小上限和文件名校验；资源引用只允许关联同组织 `resource` 内容；未关联资源的 `content_id` 按契约返回 `null`。
- S1 知识目录关联收口：内容新增可空目录外键，知识文章创建/编辑必须关联当前组织目录；父目录校验组织归属与循环；门户仅展示公开目录下的已发布文章，并按目录名称筛选；目录和内容变更统一失效门户缓存。

### 3.5 7 月 28 日执行记录

- Admin 内容编辑器已独立为完整页面，支持标准 Markdown、实时预览、Markdown 文件导入、图片与附件插入；Portal 详情使用清洗后的 Markdown HTML，不再把正文语法当纯文本显示。
- 媒体上传现可关联动态、资源和知识文章；内容发布后 API 将后台媒体引用转换为组织范围内的公开地址，图片以内联方式返回，下线后立即失去公开读取权限。
- 新增带 `integration` 构建标签的 S1 集成套件和 `scripts/run-s1-integration.ps1`。套件连接真实 API、MySQL 与 Redis，测试数据在每轮结束后自动清理。
- Compose 环境中连续三轮“创建草稿 → Portal 不可见 → 发布 → Portal 可见 → 重复发布 `409` → 下线 → Portal 不可见 → 重复下线 `409`”全部通过；每轮均直接确认列表/详情 Redis 键在状态变化后被删除。
- 资源下载边界通过：草稿 `404`、已发布 `200 image/png`、错误组织 `404`、下线后 `404`，下载字节、类型和内联响应头均与存储文件一致。
- S2 收口测试已提前完成：新增 `scripts/run-s2-integration.ps1`，以真实 API/MySQL 执行邀请创建、预览、注册接受、项目分配和里程碑完成主路径。
- S2 异常边界已自动验证：重复邀请 `409`、邀请邮箱不匹配 `400`、token 使用后 `410`/重复消费 `409`、Editor 越权 `403`、Owner 修改/移除 `409`、非法日期 `400`；邀请响应不暴露 token 哈希。
- S2 邀请管理增强已完成：Admin 可分页查看当前组织邀请及邮件状态、筛选待处理邀请并主动撤销；撤销后原 token 立即失效，重复/终态撤销返回 `409`，成功操作写入审计。
- S2/S6 基础批量邀请已提前完成：单次 1—20 条、逐项返回成功/失败、成功链接只展示一次；坏邮箱、已有成员和同批重复不会回滚其他成功邀请，Admin 支持批量录入与复制全部成功链接。
- S2/S6 多组织会话已提前完成：Admin 可列出并切换当前账户的 active 组织；切换会重新校验成员关系、轮换 Refresh Token、重新签发组织绑定的 Access Token 并写审计，刷新不回退原组织，停用后会话立即失效。
- S6 双组织演示资料与真实模型演练已于 8/5 完成：显式 Seed 可幂等准备 QUTCraft/Campus Commons、Owner 关系、独立知识/项目/申请和 Agent 定义；`agnes-2.0-flash` 两边各 1 轮均通过，引用、Prompt v2 与 6 类建议操作保持组织隔离。
- 双组织门户与产品叙事已于 8/5 收口：Admin 切换后“查看门户”携带当前组织 slug，Portal 会话持续读取对应公开 API；Campus Commons 的门户、登录、后台概览、申请样例与审核入口均改用通用协作语义，不暴露 Minecraft、白名单、玩家指标或服务器控制卡片。
- 项目成员重复添加按现有契约幂等更新角色；角色修改、移除和完成时间落库均通过。测试结束前会清理并确认临时用户、邀请、成员关系和里程碑为零残留。
- S3 已收口为纯申请审核：申请提交、批准/拒绝、并发冲突、组织隔离、审计与通知 Outbox 均有真实持久化和集成测试。
- 游戏服务器远程控制相关路由、权限、前端卡片、Mock 实现和运行时配置已于 8/17 删除；历史数据库迁移保留以维持已部署实例的迁移链兼容性。
- S3 申请查询收口：Admin 列表现支持审批状态、申请类型、最新同步状态和姓名/游戏 ID/邮箱/QQ 组合筛选，数据库侧完成组织隔离与分页；页面展示同步尝试次数、请求时间和脱敏结果，桌面及 430px 窄屏检查通过。
- S3 审核原因已接通预留数据库字段、服务层、OpenAPI 和 Admin：拒绝原因必填，通过备注可选，均限制 500 字符并仅在受保护后台展示。
- S3 集成套件已增加组合筛选、分页元数据、非法同步状态 `400`、拒绝原因必填和原因持久化用例，并与原有审批、Mock 同步、故障重试和命令注入用例一起在真实 Compose/MySQL 环境通过。
- S3 演示 seed 已从无条件启动写入改为 `DEMO_SEED_ENABLED` 显式开关：基础迁移/RBAC/可选 Owner 与演示业务数据分离；重复执行只补缺失固定 ID，不覆盖人工修改。真实 MySQL 已验证连续两次执行数量稳定，并验证容器显式启用/恢复关闭。
- S4 首片已提前完成：新增独立 Portal Manifest v1 与主题 Token 校验器，覆盖 Schema、SemVer、同源静态路径、能力白名单、MD3 回退、SRI、颜色/圆角/字体范围，并以单元测试拒绝外站、目录穿越、后台能力和 CSS URL 注入。
- S4 配置切片已提前完成：新增组织级 `portal_configurations` 持久化模型与迁移，草稿/生效 Manifest 分列存储；只有 `organization:configure` 可读取、保存和启用，保存与启用分别写入事务审计。
- Admin 设置页已从门户“假保存”切到真实 API，支持 Manifest 字段编辑、JSON 导入、同源入口预览、草稿保存和独立启用；SMTP 示例授权码先从前端常量移除，后于 7/30 被服务端只读适配器状态取代。
- `scripts/run-s4-integration.ps1` 已在真实 Compose/MySQL 通过：无令牌 `401`、外站入口 `400`、草稿不影响生效版本、刷新持久化、启用复制和两条审计事件均符合预期。
- S4 运行时加载已提前完成：新增公开配置端点，只返回再次校验的生效 Manifest；无配置或损坏时返回内置 MD3。Web 启动时以 1.8 秒上限探测同源 HTML 和 `qutc-portal-id` 标记，超时、404、错误类型或标记不符均保留 MD3。
- Nginx 对 `/portals/` 禁用 SPA fallback，不存在的门户资源真实返回 `404`；Admin、认证、邀请与申请页面不参与门户替换，公开页可用 `?portal=md3` 强制回退。
- S4 集成套件已扩展验证“草稿不进入公开运行时 → 启用后公开可见 → 数据库存储损坏自动返回默认 MD3 → 管理员永久恢复默认 MD3”；公开 API、Nginx 404 和默认门户均完成运行态检查，OpenAPI 与 Go 对齐为 58 个操作。
- 已补通用自定义门户包结构、入口标记、Portal API 边界、CSP 资源规则和发布检查表；运行时失败会写入当前会话记录与浏览器告警，Admin 提供带事务和审计的“恢复默认 MD3”入口。
- Web 已启用 CSP、Referrer-Policy、Permissions-Policy、`nosniff` 与防嵌入响应头；正常页面和 `/portals/` 真实 404 均携带安全头，现有默认门户兼容检查通过。
- S5 契约门禁已提前完成并持续扩展：8/2 质量汇总闭环时为 79 个操作/146 个 Schema/71 个前端请求；截至 8/5 已扩展为 92 个操作、169 个 Schema 和 84 个真实请求，Gin、OpenAPI 与四个前端 client 继续完全对齐。
- 已提供可导入 Apifox 的 26 请求核心集合和无凭据环境模板，覆盖健康、登录、内容“草稿 → 发布 → 归档”、“申请 → 后台检索 → 拒绝”、智能体配置/知识生成及活动策划“生成 → 查询 → 历史 → 人工评分/汇总 → 人工批准”；集合请求与敏感变量由仓库脚本自动校验。
- 统一 `run-quality-gate.ps1` 已接入契约、Go、前端、路由冒烟和 S1—S6 Compose 集成套件；GitHub Actions 无外部服务门禁和 10 条 SPA 路由/CSP/门户 404 冒烟已落地。
- S5 安全切片已提前完成：认证/邀请、公开申请、资产上传和服务器命令按“客户端 IP + 路由”限流，拒绝任意代理头伪造来源；`429`、频率响应头和单实例边界已同步 OpenAPI。
- S5 可观测性切片已于 7/30 提前实现：安全 Request ID 传播、脱敏 JSON 访问日志、`/healthz`/`/readyz` 分离、组织隔离审计查询 API 与 `/admin/audit` 页面均已完成。
- S5 备份恢复切片已于 7/30 提前完成：脚本短暂停写后导出 MySQL 和本地媒体卷，生成逐表数量、逐文件 SHA-256 与整体文件校验；真实演练在随机临时数据库/卷恢复并核对 21 张表、2 个媒体文件，随后确认临时目标和敏感备份均已清理。
- 上传在 multipart 解析前限制整个请求为 11 MiB，再以 10 MiB 文件上限和 MIME 白名单二次校验；生产启动拒绝占位 JWT、通配 CORS、短引导密码和演示 seed，仓库门禁扫描高置信度密钥及前端服务端密钥配置。
- CORS 单测与运行态检查均通过：配置源预检返回 `204` 和明确来源，未授权来源返回 `403`；限流窗口、上传 `413`、命令注入、RBAC 越权及 S1—S4 完整回归通过。
- 产品范围调整：取消第一方 Minecraft 主题门户与游戏服务器远程控制，保留通用自定义门户标准、默认 MD3 回退和通用申请审核。该调整不影响 G4 的安全与运行时验收。
- 申请审批、通知 Outbox、错误码与审计语义已整理为独立 API 规范。
- AI 智能体 AI-0 后端基础与配置页已于 7/30 提前完成：`ModelProvider`、AgentDefinition/AgentRun/引用快照、组织隔离知识检索、异步生成、取消、RBAC/审计和 OpenAPI/Apifox/前端类型客户端均已落地；`/admin/ai` 可查看脱敏供应商状态并持久化组织启停、超时、小时配额、引用和上下文上限。人工确认创建草稿仍按 AI-1 推进，AI 禁止直接发布、审批、修改角色或执行任意外部命令。
- 本轮 `go test ./...`、集成测试编译、`git diff --check` 通过；Docker Compose 的 Web、API、MySQL、Redis 已恢复运行。

### 3.6 7 月 30 日执行记录

- 媒体服务新增统一 `Store` 适配层，默认本地受控目录与 S3 兼容对象存储共享同一上传、下载、删除语义；MinIO 不再是“Compose 中启动但 API 未使用”的孤立服务。
- 上传对象按组织与 UUID 生成内部 Key；`media_assets` 新增 `storage_driver`，公开 API 仍只返回受控下载地址，不返回驱动、bucket、对象键或长期凭据。
- API 启动时校验存储驱动配置；S3 模式要求服务端 Access Key、Secret Key、bucket 和无协议 endpoint，生产环境拒绝 MinIO 示例凭据。
- 上传保持 11 MiB 请求/10 MiB 文件双重边界；对象写入失败不落元数据，元数据失败会补偿删除对象。下载改为统一流式响应，存储不可用明确返回 `503`。
- 新增本地存储遍历防护、往返/删除和旧绝对路径兼容单测；新增真实 MinIO `Put → Open → Delete → 404` 集成测试与独立运行脚本。
- Compose 与环境模板已提供 `STORAGE_DRIVER=local|s3` 全量配置；媒体存储配置、切换限制和迁移边界整理为独立规范。
- 运行态验证已通过：默认 local 驱动完成 S1—S4 全量回归；MinIO 驱动完成“Admin 上传 → 元数据确认 s3 → 发布 → Portal 下载同字节 → 下线 404 → 对象/数据清理”，统一质量门禁以 `-Integration -StorageIntegration` 返回 `QUALITY_GATE_OK`。
- 成员邀请新增 `disabled`/`smtp` 邮件适配器：邀请先提交、邮件事务外投递，失败不会回滚邀请；每个邀请独立保存 `disabled/pending/sent/failed`、尝试次数和脱敏错误。
- Admin 成员页会按真实投递结果显示“已发送 / 失败 / 未启用”，邮件失败可轮换 token 并重试，旧链接立即失效；系统设置页移除 SMTP 密码假表单，只读取服务端安全状态。
- SMTP 主机、认证信息和授权码只允许由后端部署变量注入；状态 API 不返回主机、用户名或密码，生产环境拒绝明文 SMTP。
- OpenAPI、Go 路由和前端请求契约在邮件切片后扩展为 68 个操作/61 个真实请求，后续智能体配置切片继续扩展为 70/63；邮件适配器协议单测、配置校验和 S2 禁用降级集成断言已补齐。
- 重建后的 Compose 已完成路由冒烟与 S1—S4 全量集成回归；受认证邮件状态接口返回 `disabled/false/false`，S2 确认邀请链接在无 SMTP 工况下仍可完整使用并清理投递记录。
- S2 成员状态收口：`RequireAuth` 现在逐请求校验全局账户与当前组织成员关系；角色降级立即影响 RBAC，停用后旧 Access Token 返回 `401 auth.session_inactive`，Refresh Token 在停用事务中撤销。
- 组织成员状态不再同步覆盖全局 `users.state`；Admin 只允许写 `active/disabled`，`invited` 保留给邀请流程。页面增加停用影响确认，Owner 降级/停用继续由服务端拒绝。
- 扩展后的 S2 Compose 回归已通过“Editor 可读内容 → 降级 Member 后旧 token 即时 `403` → 停用后旧 Access/Refresh 均 `401` → 全局账户仍 active → 重新启用并登录恢复 Editor 权限”，并确认三类成员历史事件及临时审计数据完整清理。
- 备份恢复脚本与运维手册已完成：`backup-compose.ps1` 默认暂停 API 写入，`verify-backup-restore.ps1` 拒绝不完整/校验失败的备份并只恢复到隔离目标；`run-backup-restore-rehearsal.ps1` 可供部署门禁一键演练。
- 当前 Compose 本地存储真实演练通过：21 张表的集合与逐表行数一致，2 个媒体文件逐文件 SHA-256 一致；临时数据库、卷和系统临时备份均已清理，源 API 恢复 healthy。

### 3.7 7 月 31 日执行记录

- AI-1 内容协作工作台已接入完整 Markdown 编辑器：“从知识生成”支持当前组织知识检索、跨检索保留选择、组织引用上限、任务描述、异步状态轮询与取消。
- 生成结果使用既有安全 Markdown 渲染器，提供“生成结果 / 与当前正文并排对比 / 固定引用详情”三种视图；开发 Mock、真实模型和禁用状态始终明确标识。
- 人工确认分为两级：“应用到当前编辑器”只修改本地表单、不保存；“确认并创建新草稿”再次确认后调用既有 CMS 创建接口，永远不会自动发布。
- 创建草稿前会补齐模型输出遗漏的引用 ID，保证来源标识进入正文；知识内容、模型输出和 Markdown 均继续按不可信数据处理。
- S6 集成门禁扩展为连续三轮“知识 → AI Markdown → 生成阶段零内容写入 → 人工确认草稿 → Portal 草稿 404 → 人工发布可见且保留引用 → 下线 404”，并清理运行、引用、内容和审计夹具。

### 3.8 8 月 1 日收口记录

- 组织名称、简称、标语、介绍、公开邮箱、社交链接和公开状态已由硬编码迁移到数据库；Admin 系统设置可修改，Portal 只读取公开组织，保存会写事务审计并立即失效公开缓存。
- 内容、知识目录、项目、项目成员、里程碑、成员状态和媒体上传的成功写入均与审计事件置于同一数据库事务，避免“业务成功但无审计”。
- Refresh Token 已迁移到 `HttpOnly + SameSite=Strict` Cookie，生产环境额外启用 `Secure`；Access Token 只保存在页面内存，刷新和退出不再从 JSON 或 Web Storage 读取 Refresh Token。
- API 不再每次启动执行 `AutoMigrate`：001—009 SQL 随二进制内嵌、按序执行并写入 `schema_migrations`，旧数据卷自动登记 001—008 基线后执行后续升级。
- OpenAPI、Go 与 Web 在 8/1 已扩展并对齐为 75 个操作/68 个真实请求；Playwright 已覆盖桌面和移动端 Portal、登录、组织设置、Markdown 编辑器滚动回归和活动策划工作台入口，8/2 又随历史、评分和质量汇总接口扩展至 79/71。
- 演示数据新增 `qutcraft` 与 `generic` 两套可选 Profile，比赛通用叙事不再依赖 Minecraft 文案；已存在的人工内容不会被 seed 覆盖。

### 3.9 8 月 2 日活动策划安全收口

- `activity-planner` 系统策略升级为 v2：活动简报和知识来源统一编码为不可信 JSON，明确拒绝输入中的角色伪造、规则覆盖、工具越权、密钥泄露和绕过人工批准指令。
- 011 迁移与启动 seed 会把已有组织的静态智能体定义同步到 v2，同时保留组织自己的启停和配额配置。
- S6 真实 Compose/MySQL 已覆盖跨组织资料 `404`、Editor 批准 `403`、缺失项目依赖 `400`、部分批准、重复批准 `409`、Prompt Injection 标记不进入 Mock 方案以及 Request ID 逐对象审计。
- 故障回滚通过不存在的操作者触发真实外键失败，确认活动方案恢复 `ready`，且项目、公告草稿和本次审计记录均为零；失败不会留下半成品。
- 建立 10 组校园活动评测集和独立 `agent-eval` 工具；Mock 与 OpenAI-compatible 真实模型使用同一 Prompt v2、结构、固定引用、越权宣称和注入标记口径。
- `agnes-2.0-flash` 于 8/2 完成首轮真实评测：自动基线 10/10，通过平均延迟 11,883 ms、P95 15,279 ms；报告不保存生成全文或凭据。
- 活动策划已新增组织隔离分页历史、当前方案刷新恢复和按“方案 + 评审人”唯一的五维人工评分；评分更新写审计但不触发业务执行，S6 覆盖 Editor 评分、非法分值、独立读取、历史可见性和组织级质量汇总。
- 管理端新增活动方案质量证据概览，展示评价/方案数量、五维均分和真实模型/Prompt 分组；汇总接口不返回评审人或评语。新增真实 API 连续演练脚本，报告不保存生成正文且明确把人工评分与批准留为 `pending`。
- `agnes-2.0-flash` 真实 API 连续演练已完成 3/3：三轮均进入 `ready`、保留固定引用并返回 6 类建议操作，平均 13,193 ms、最慢 21,558 ms，合计输入/输出 Token 为 2,196/4,675。三个方案保留在历史中等待团队人工评分与批准。
- 活动策划历史继续归属于活动策划工作台，不在 Admin 侧栏拆分独立模块；页面新增“待我评分”队列，历史摘要只返回当前登录用户自己的评分状态与均分，其他评审人的明细保持隔离。
- 修复活动方案评分 `PUT` 被 CORS 预检遗漏导致浏览器显示 `Failed to fetch`：服务端允许方法已补齐 `PUT`，并以单测和 Compose 运行态预检确认 `204`、明确来源及完整方法列表。

## 4. 执行时间表

### S0 · 7 月 25 日 — 7 月 27 日：基线校准与工程止血

**目标：** 让仓库文档、契约和实际运行状态一致，为后续开发建立可测基线。

| 日期 | 主要任务 | 可交付结果 |
| --- | --- | --- |
| 7/25 | 建立功能地图 v2、重排 schedule、标出页面/API/持久化错位 | 功能状态、优先级、依赖和扩展版本可查。 |
| 7/26 | 用 Compose 跑 Portal/Admin/Auth/CMS 冒烟；记录真实失败；检查 OpenAPI 与已注册 Go 路由 | 一份按 P0/P1 排序的问题清单；远程模式不靠 Mock 掩盖失败。 |
| 7/27 | 修复阻断级契约错位；至少为申请提交定义正式 OpenAPI 和数据库方案；补开发检查脚本 | 后续实现不再凭前端类型猜接口。 |

**阶段门 G0：**

- Web、API、MySQL、Redis 能按 README 启动。
- 未登录访问 Admin 被阻止，权限失败使用统一响应。
- Portal 核心页面在远程 API 模式有明确加载、空、错状态。
- G-01 至 G-08 都有归属阶段，不存在“默认算完成”的功能。

> 7 月 26 日复核：运行态冒烟与全新镜像构建均已通过，G0 的“按 README 启动”验证完成。

### S1 · 7 月 28 日 — 8 月 2 日：内容、资源与知识闭环

**目标：** 把已存在的 CMS 能力从“能操作”收口成稳定的公开内容生产线。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 7/28 | 对齐内容 DTO、状态机和权限；补创建/编辑/发布/下线服务测试 | 非法状态被拒绝；无发布权限无法发布。 |
| 7/29 | 完成 Portal 内容完整阅读路径、统一 404/空错态；确认动态与项目领域分离 | 发布内容可完整消费；项目不会混入动态列表。 |
| 7/30 | 完成资源元数据、上传类型/大小限制、下载授权、内容引用及 local/MinIO 可替换存储 | Portal 不获得对象存储凭据或长期内部地址。 |
| 7/31 | 接入知识目录管理界面；文章与目录关联；修复目录缓存失效 | 后台可维护目录，门户只显示公开文章。 |
| 8/1 | 覆盖发布、下线、资源下载、缓存失效的集成用例 | 内容变更后 Portal 不读取陈旧数据。 |
| 8/2 | Compose 全链路演练、缺陷修复和阶段演示 | 内容闭环连续三次成功。 |

**阶段门 G1：**

- 后台发布后 Portal 可见，下线后不可见。
- 草稿、作者内部字段和对象键不进入 Portal API。
- 资源成功/失败下载、知识空目录和缓存失效均有可验证路径。

**本阶段切线：** 定时发布、复杂富文本、全文搜索、行级 diff 进入 `v0.2`；内容修订快照/恢复与下载统计首版已提前补齐。

### S2 · 8 月 3 日 — 8 月 8 日：成员与项目协作

**目标：** 补齐组织数字化管理的核心协作能力，不扩展成完整项目管理软件。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/3 | 设计邀请模型、一次性 Token、过期/重复策略和 OpenAPI | 邀请不会创建重复成员，Token 不明文持久化。 |
| 8/4 | 实现邀请创建、查询/接受与成员关系落库；可选邮件适配器已于 7/30 提前完成 | 无 SMTP 时仍可复制链接；SMTP 成功/失败均有真实状态，失败可轮换链接重试。 |
| 8/5 | 接通 Admin “邀请成员”；补角色修改、停用和 Owner 保护 | 原占位按钮成为真实操作，越权写入被拒绝。 |
| 8/6 | 在项目管理中接入成员列表、添加、改角色和移除（已于 7/26 提前完成） | 只能添加当前组织活跃成员；Owner 不可移除。 |
| 8/7 | 接入里程碑创建、编辑、完成、删除及日期校验（已于 7/26 提前完成） | 里程碑计数与列表一致，错误状态可见。 |
| 8/8 | 用虚拟社团数据演练“邀请 → 加入 → 分配项目 → 完成里程碑” | 主路径与权限失败路径均通过。 |

**阶段门 G2：**

- 邀请成员按钮可完成真实邀请流程；邀请 token 不明文落库，接受后不能重复使用。
- 成员停用后不能继续使用受保护能力。
- 项目成员和里程碑不再是“只有接口、没有产品入口”。
- 项目成员与里程碑的远程 API 成功路径已在 Compose 环境跑通，临时测试数据可回收。
- “邀请 → 加入 → 分配项目 → 完成里程碑”主路径与 token 重用、负责人保护等权限/冲突路径均已验证。

**本阶段切线：** 批量项目成员分配、成员技能档案、项目任务看板、评论、工时和甘特图进入 `v0.2`；S2 后续只补服务级测试和异常路径，不扩展协作模型。

### S3 · 8 月 9 日 — 8 月 15 日：申请审批与 Minecraft 适配

**目标：** 完成 QUTCraft 特色闭环，同时保持通用核心和游戏服务器解耦。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/9 | 增加申请数据库模型、迁移、Portal 提交接口和校验 | Portal 提交后后台立即可见，重启不丢失。 |
| 8/10 | 申请分页、组织隔离、组合筛选、审核原因和显式演示 seed（已于 7/28 提前完成） | 不同组织数据不可串读；演示数据可幂等创建且不覆盖已有修改。 |
| 8/11 | 审批事务、并发冲突、审批审计和幂等语义 | 同一申请不能被重复决定；返回 `409`。 |
| 8/12 | 完成邀请预创建账户、`invited` 成员状态和角色占位 | 发出邀请后成员列表立即可追踪；占位账户不能登录。 |
| 8/13 | 完成邀请注册激活、已有账户接受与撤销清理 | 激活复用原账户/成员 ID；撤销不影响活跃账户和其他组织。 |
| 8/14 | 收口纯申请审核事务、审计和通知 Outbox | 重复审批返回 `409`；通知失败不污染审批结果。 |
| 8/15 | 删除游戏服务器远程控制运行时、API、权限、前端与 Mock | 已发布 API 契约和页面不再暴露该能力。 |

**阶段门 G3：**

- 申请闭环使用真实数据库，不依赖进程内状态。
- 审批人、原因、原/新状态、`request_id` 和通知结果可追溯。
- 邀请预创建、激活、已有账户接受和撤销清理均可重复验收。

**本阶段切线：** 补充材料、批量审批和自定义审核表单进入后续版本。

### S4 · 8 月 16 日 — 8 月 21 日：自定义门户运行时

**目标：** 把 Portal Manifest 从文档变成可演示的产品能力。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/16 | 冻结 Manifest v1 与 Portal API 能力清单；实现 Schema 与主题 Token 校验（已于 7/28 提前完成） | 未声明能力、错误版本、非法 URL、目录穿越和主题注入被拒绝。 |
| 8/17 | 建立门户配置持久化 API、RBAC 与变更审计（已于 7/28 提前完成） | 只有授权管理员可以保存/启用门户。 |
| 8/18 | 实现 Admin 预览和启用流程，替换设置页的本地“假保存”（已于 7/28 提前完成） | 刷新后配置仍在；预览不影响当前线上门户。 |
| 8/19 | 实现运行时加载、超时、资源错误和默认 MD3 回退（已于 7/28 提前完成） | 自定义入口失败时网站仍可访问。 |
| 8/20 | 补通用自定义门户接入说明、运行时错误记录与恢复入口（已于 7/28 提前完成） | 第三方页面只消费 Portal API；临时回退和永久恢复均可用。 |
| 8/21 | 安全边界、CORS/CSP 和兼容检查；默认/自定义运行时演练（已于 7/28 提前完成） | 安全响应头覆盖正常页与门户 404；切换门户不改变 Admin URL、权限或数据。 |

**阶段门 G4：**

- 默认 MD3 永远可回退。
- 自定义门户和 MD3 使用同一公开数据契约。
- 自定义门户无法读取草稿、成员邮箱、审批和服务端密钥。
- 管理员可原子恢复默认 MD3，恢复动作写入审计。
- 门户静态资源和失败响应均受 CSP 与基础安全响应头保护。

**本阶段切线：** 主题市场、在线代码编辑、任意脚本插件、A/B 测试进入 `v0.3+`。

### S5 · 8 月 22 日 — 8 月 26 日：质量、安全与可部署性

**目标：** 从“功能可演示”推进到“可复现、可排错、可交接”。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/22 | OpenAPI lint、Go 路由差异检查、Apifox 核心集合（已于 7/28 提前完成并持续扩展） | 当前 92 个操作、169 个 Schema、84 个前端请求和 26 个核心集合请求进入自动契约门禁。 |
| 8/23 | Go 单测、关键服务集成测试、前端构建和路由冒烟（已于 7/28 提前完成并持续扩展） | 单命令完整门禁、GitHub Actions、S1—S6 集成及 10 条 SPA 路由冒烟均已接入。 |
| 8/24 | 越权、CORS、限流、上传、命令注入、日志脱敏和密钥扫描（除结构化日志脱敏并入 8/25，其余已于 7/28 提前完成） | 安全边界已有单测/集成/运行态验证；敏感配置不进入浏览器源码，生产占位配置无法启动。 |
| 8/25 | 结构化日志、健康检查、审计查询、MySQL/媒体备份恢复演练（均已于 7/30 提前实现） | 问题可用 `request_id` 关联访问日志与组织审计；本地卷备份已在隔离数据库/卷验证可恢复。 |
| 8/26 | 新机器 Compose 部署、端口覆盖、故障降级与三次全链路回归 | 不依赖开发者机器上的隐式配置。 |

**阶段门 G5：**

- `go test`、前端构建、OpenAPI 校验和核心冒烟全部通过。
- Compose 环境中平台核心闭环连续三次成功；AI 活动服务闭环按 S6 单独验收。
- 仓库不含生产密码、真实成员隐私、SMTP/MinIO 密钥。
- 阻断级和高风险缺陷清零，其他问题进入已知问题清单。

### S6 · 8 月 1 日 — 8 月 24 日：服务创新类 AI 活动运营主线

**目标：** 把比赛作品从“通用 CMS + AI 写作”收敛为“面向校园组织的 AI 活动策划与运营服务”，同时保持 CMS 状态机、RBAC 和公开边界不被绕过。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/1—8/3 | 冻结服务创新类叙事、结构化活动简报、activity-planner、活动工作台和受控建议操作契约 | 已完成第一片：活动需求、知识选择、生成方案、逐项批准项目/里程碑/公告草稿；OpenAPI/Go/Web 编译对齐。 |
| 8/4—8/10 | 完成活动方案 Compose 全链路、部分批准、重复批准、越权、事务回滚和 Prompt Injection 测试（已于 8/2 提前完成） | ✅ 失败不产生半成品；跨组织资料统一拒绝；所有创建对象可由 Request ID 追踪。 |
| 8/11—8/17 | 接入真实模型，建立 10 组校园活动评测集，校准方案结构、引用正确性、延迟和失败提示（首轮自动基线、评分持久化/汇总与演练工具已于 8/2 提前完成） | 真实模型自动检查 10/10；连续三轮真实 API 生成通过；人工评分与批准必须由操作者完成，正式演示不使用 Mock 冒充真实模型。 |
| 8/18—8/21 | 完成多组织切换和基础批量邀请，并用 QUTCraft 与通用社团两套资料演练活动闭环（均已于 8/5 提前完成） | ✅ 同一用户可切换组织且 Refresh、数据与 RBAC 不串租户；批量邀请逐项返回；真实模型双组织演练 2/2 通过。 |
| 8/22—8/24 | 完成移动端、空错态、无模型降级、视觉回归与最终功能冻结 | 主演示连续三次成功；阻断缺陷清零。 |

**阶段门 G6：**

- 比赛环境至少配置一个真实可调用的模型供应商；开发 Mock 必须显式标识，不能冒充真实生成。
- AI 只通过类型化工具访问 CMS，不直接查询任意数据库、不执行任意 HTTP/SQL/Shell。
- 有效权限为“当前用户权限 ∩ 智能体工具白名单 ∩ 本次运行策略 ∩ 人工确认”的交集。
- 引用的知识资料必须属于当前组织且对当前用户可见；回答和草稿保留来源标识。
- AI 不能直接发布、下线、删除内容，不能审批申请、修改角色、发送外部消息或执行任意外部命令。

**本阶段切线：** Portal 公开问答、自动周报、多智能体协作、向量数据库专项优化、通用工作流设计器与 AI 自动执行高风险动作进入 `v0.2+`。

**当前实施状态（8/5）：** 原内容协作闭环继续保留；activity-planner、010—015 迁移、独立 Admin 向导、带引用方案、历史恢复、人工评分/质量汇总和受控批准接口已完成。014 增加组织邀请模板、审批通知 Outbox、默认组织偏好、内容修订、下载统计和 AI 引用正文，015 为既有内容补齐 v1 修订基线。基础批量邀请、多组织会话、通用组织加入表和单机 AI 持久化队列均已接入。Compose S2 验证切换、Refresh 上下文保持、目标组织业务隔离、越权拒绝和成员停用即时失效；显式双组织 Demo Seed 与真实模型演练已验证 QUTCraft/Campus Commons 各 1 轮通过。Compose S6 已覆盖方案生成、跨组织资料隔离、Prompt Injection、RBAC 越权、依赖校验、评分与汇总、部分/重复批准、Request ID 审计与真实事务回滚。10 组校园活动数据集已分别通过 Mock 和 `agnes-2.0-flash` 自动基线，真实 API 连续演练达到 3/3；当前剩余收口包括通知多渠道/多实例治理、行级版本 diff、项目任务、全文搜索、新机器复现、最终故障/视觉回归和交付材料。

### S7 · 8 月 25 日 — 8 月 31 日：交付冻结与比赛材料

**目标：** 把可运行系统整理成评委能理解、团队能复现的正式交付。

| 日期 | 主要任务 | 验收点 |
| --- | --- | --- |
| 8/25—8/27 | 生成两套演示资料；更新 README、部署、API、架构图和已知问题；录制主演示与无网络应急视频 | 新成员可独立启动；智能体主演示在限定时间内完成。 |
| 8/28—8/29 | 完成设计说明书、功能说明、源码清单、答辩问答和材料交叉校对 | 材料与真实实现一致；AI/Mock/真实执行有明确标识。 |
| 8/30 | 最终回归、代码冻结、发布说明、`v0.1.0-competition` 标签并完成正式提交 | 仓库、文档、演示材料和已知问题完整归档。 |
| 8/31 | 仅保留为平台故障或材料上传异常缓冲，不安排新增功能 | 不因最后一天新增改动破坏已验收版本。 |

**最终阶段门 G7：**

- 发布标签只包含通过 G5 和 G6 的代码。
- 通用组织演示突出数字化管理，QUTCraft 演示突出真实社团场景和服务器适配。
- 演示中明确区分“真实模型生成”“开发 Mock”“规划功能”。
- AI 内容闭环在无外部模型时有明确失败/降级界面，但比赛正式演示不得用 Mock 冒充真实模型。

## 5. 扩展功能如何进入排期

新增需求先登记到 [功能地图 v2](docs/product/feature-map-v2.md)，再按以下规则进入迭代：

| 类型 | 进入条件 | 安排方式 |
| --- | --- | --- |
| P0 缺陷 | 阻断核心闭环、安全边界或数据正确性 | 立即进入当前阶段，必要时挤出同阶段 P1。 |
| P1 增强 | 当前阶段门有明确帮助，且预计不改变核心契约 | 放入阶段预留时间；未完成进入下一阶段首日评估。 |
| P2 扩展 | 依赖稳定且不影响 `v0.1.0-mvp` | 只做设计/接口预留，开发进入 `v0.2+`。 |
| 外部集成 | 有测试环境、凭据管理和失败降级方案 | 以 Adapter 接入，不允许绑死核心服务。 |

每个扩展需求至少写明：目标用户、价值、依赖、权限、公开/内部数据边界、验收方式、失败降级和计划版本。缺少这些信息的需求不进入开发中状态。

## 6. 延期切线与保底方案

如果进度落后，按下列顺序收缩，不削弱安全和数据正确性：

1. 保留纯文本/Markdown，延期复杂富文本和协同编辑。
2. 保留基础资源上传下载，MinIO 使用可替换 profile；延期分片、版本报表和对象迁移。
3. 保留后台可追踪的审批状态、持久化通知 Outbox 与申请人邮件结果；延期申请人自助查询、多渠道和多实例 worker；绝不在前端保存邮件密码。
4. 保留申请审核、审计和通知 Outbox，延期补充材料与复杂工作流；绝不以浏览器端逻辑替代服务端授权。
5. 保留通用 Manifest 标准和默认 MD3 回退，延期第一方主题、主题市场、在线编辑器和灰度发布。
6. 保留项目成员与里程碑，延期任务看板、评论、甘特图和工时。
7. 保留单组织完整隔离模型，延期面向公网的多租户运营能力。

平台核心闭环、AI 活动服务闭环、RBAC、审计、公开数据边界、持久化和回退机制不在可裁剪范围内。

## 7. 协作与检查节奏

### 每日

- 开始开发前确认当天切片的 OpenAPI、权限和完成定义。
- 结束前更新对应 Issue/功能状态，记录已验证命令和未解决风险。
- 不把仅在 Mock API 模式可用的页面计入远程环境完成度。

### 每个阶段

- 阶段开始：只拉取该阶段 P0/P1，标记责任人与依赖。
- 阶段中点：运行一次 Compose 集成，不等到最后一天集中联调。
- 阶段结束：按阶段门逐项验收；未通过的 P0 自动进入下一阶段首位。

### 代码与契约

- 跨端接口先改 OpenAPI，再改 Go 与 TypeScript client。
- 涉及权限、迁移、上传、服务器适配和门户配置的变更必须复核。
- 合并前至少运行与改动相关的格式检查、测试、构建和契约校验。
- 任何密钥只通过后端环境或受控密钥系统提供，不进入 `VITE_*`、前端常量或 Git。

## 8. 进度报告模板

每次阶段同步只回答以下问题，避免用页面数量代替真实进度：

```text
阶段 / 日期：
本次完成的业务切片：
对应功能地图编号或领域：
已验证：页面 / OpenAPI / Go / 数据库 / RBAC / 测试
仍缺失：
阻塞与外部依赖：
是否影响阶段门：是 / 否
需要延期到的版本：
```

## 9. 统一完成定义

一个功能只有满足以下条件才能计入 schedule 完成度：

1. 用户成功、空、失败、冲突和权限路径有明确表现。
2. OpenAPI、Go API、前端 client 和持久化模型一致。
3. 权限由服务端执行，Portal 不泄露内部字段。
4. 关键状态不会因进程重启丢失；Mock 有明确标识。
5. 至少有一条自动化验证，核心闭环在 Compose 中通过。
6. 相关 README、功能地图、Swagger/Apifox 和变更说明已同步。
