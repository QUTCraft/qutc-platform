# 安全策略

## 报告漏洞

如果你发现安全漏洞，请不要通过公开 Issue 报告。请发送邮件至项目维护者，并在邮件中说明：

- 漏洞的类型和影响范围
- 复现步骤
- 可能的修复建议

维护团队将在 72 小时内确认收到报告，并在修复完成后公开致谢（如果你希望匿名，请在邮件中说明）。

## 安全最佳实践

- 永远不要在前端代码、环境变量（`VITE_*`）或 Git 中存储密钥、Token 或密码
- 所有密钥只通过后端环境变量或受控密钥系统提供
- 生产环境必须替换所有默认密码和占位密钥
- 提交前运行 `python scripts/scan-secrets.py` 扫描密钥泄露
